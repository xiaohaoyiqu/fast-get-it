# 后续测试和开发记录

本文记录第一版本地内容下载器后续怎么测试、怎么继续做。

## 2026-09-19 Bluesky 与 Instagram 第一轮接入

- 修复 Windows Python 启动环境差异：实际 `py` 默认使用 Python 3.12，已为该解释器安装 `requirements-app.txt`，其中 PyCryptodome 提供 `Crypto` 导入。`py -3.12 .\run_software.py modules` 已能完成模块装载。
- Bluesky 内置适配器标记为 `ready`：使用公开 AppView API 接入资料、用户搜索、公开关注、作者媒体、帖子搜索、单帖图片和 HLS 视频下载。真实代理环境下已读取公开资料、3 个用户候选、3 个公开关注和 10 条作者媒体动态，并从官方账号动态下载一张 1,302,572 字节图片到临时目录。当前代理出口对帖子全文搜索返回 403，软件会说明该局部限制，不影响其他公开读取。
- Instagram 内置适配器标记为 `alpha`：支持严格账号/帖子/Reels/TV 目标、Open Graph、JSON-LD 和嵌入 JSON 媒体解析、JSON/Netscape/Cookie Header 导入及图片/视频下载。公开主页真实请求返回登录限制页面，因此没有将普通账号关注或无 Cookie 主页下载标记为可用。
- 对照 `油猴脚本/bluesky-media-downloader.user.js` 和 `油猴脚本/instagram-media-downloader.user.js`，桌面端复用其目标/媒体识别思路，接入统一任务、历史、代理、下载库和黑名单；浏览器脚本继续处理当前登录页面的轻量下载与临时队列。
- Instagram 主页用户名和帖子/Reels 短代码已加入跨平台黑名单识别；相似搜索命中后可按作者或作品规则过滤。Instagram 普通账号关注列表仍显示“需登录，规划”并禁用开始任务。
- 修复滚轮递归绑定可能导致桌面启动 `RecursionError`，并修复 Instagram 规划项未正确禁用开始按钮。`1080×680` 桌面烟雾检查覆盖 Bluesky 关注/单帖与 Instagram 账号/规划关注的按钮状态。
- `compileall`、模块装载和完整 214 项离线测试通过；Python 3.12 下 `Crypto 3.23.0` 可导入，软件代理仍为 `http://127.0.0.1:6987`。

## 2026-09-18 跨平台作者黑名单

- 核对 Twitter/Pixiv 作者资料外链、FANBOX 创作者主页和 Bluesky 账号 URL 的实际结构后，新增按“平台 + 精确账号”保存的公共黑名单。资料卡可屏蔽当前作者，并可人工确认是否连带屏蔽直接外链的其他平台账号；普通帖子、作品链接不会被误当成账号关联。支持按组移除，原始关注/收藏缓存及已下载文件不受影响。
- 候选列表和候选导出过滤黑名单；任务创建前拒绝已屏蔽账号与可由 URL 识别作者的帖子。Pixiv 作品、FANBOX 和 Sketch 帖子在取得作者后再跳过。任务运行时注入黑名单路径，不改变持久化的用户任务选项。当时 Bluesky 仍是规划模块；handle 与 DID 不做未经验证的自动合并。
- 新增 4 项黑名单回归，覆盖跨站主页识别、帖子与作品边界、显式外链关联/移除、任务创建拦截及 Pixiv/FANBOX 下载前拦截。完整离线回归 161 项通过；隐藏窗口核对了新控件，并使用临时黑名单实际验证 Twitter → Pixiv 外链确认流程。

## 2026-09-18 跨平台候选名单与 Pixiv 导出

- 对照旧 PixivUtil2 插件式菜单、`历史资料/原始需求.txt` 的“收藏作品转 CSV 给其他平台使用”要求与新版候选页，把通用名单导入/导出写入 `software_app/core/candidate_lists.py`，并接到候选页。TXT、CSV、JSON 可导入为当前平台候选，CSV/JSON/TXT 可导出，均不会自动触发下载。平台字段及 Pixiv/Twitter 等已知站点 URL 做一致性校验；CSV 处理 `@账号` 等表格公式前缀的往返。
- Pixiv 作品/小说收藏导出读取完整本地缓存并应用当前筛选，不截断为界面显示的前 1000 项。候选数据在接口提供时记录页数、点赞、收藏和浏览量；未提供的热度字段留空。旧批任务语法、FANBOX 列表来源和动图存量重新编码仍不是公共名单功能的一部分。
- 新增 4 项名单/导出测试；完整离线回归 157 项通过，`compileall` 与隐藏窗口按钮烟雾测试通过。完整回归曾两次遇到 Windows 对新解压目录的瞬时重命名拒绝；公共归档提交仅针对 Windows 的访问/共享占用错误做有限次短暂重试，之后完整回归通过。

## 2026-09-18 Pixiv 收藏完整性和本地筛选

- 对照 Pixiv 帮助中心及旧 PixivUtil2 的收藏分页/标签行为，修复“更新我的收藏”误用单任务作品上限的问题。更新现在独立分页，最多读取 10000 条；站点总数、公开/私密范围及页数不完整时标记部分缓存，并保留同账号旧记录，避免短结果覆盖旧收藏。
- 收藏缓存更新始终获取未筛选的原始候选；作品 ID、作者、标签、标题及收藏/发布时间范围改为候选页本地筛选，筛选不改写缓存。日期缺失的条目在启用对应日期范围时不混用另一种日期。
- 当前 Cookie 可识别为另一账号时，不展示旧账号的收藏缓存；未配置 Cookie 时仍允许离线查看上次缓存。
- Pixiv 静态作品没有返回图片页或原图地址时任务报错，避免零文件假完成；已存在的文件与 AI 过滤数在任务摘要中分别报告。目标 URL 解析要求 Pixiv/FANBOX 域名边界。
- `E:\aimodel` 没有发现可直接迁入的 Pixiv 代码；相关浏览器工具可供后续在线回归，但真实登录、私密收藏和下载仍需可用网络及有效账号会话。
- 本机已用当前保存的会话和显式代理做在线验证：Pixiv 资料/头像检查通过；作品收藏公开/私密分页返回 316 个去重作品，接口声明总数 315，小说收藏为 0。分页验证没有覆盖本机收藏缓存；另从收藏候选中选取一件作品，下载到系统临时目录并得到 2 条文件记录，随后清理临时文件。针对“接口总数略低于实际返回数”的情况，翻页在满页时继续探测下一页。
- 新增收藏超 100 条分页、公开/私密范围不完整保护、部分缓存保留、字段和双日期筛选、满页继续探测、空图片页报错等离线回归。完整测试执行 153 项，隐藏 Tk 窗口确认收藏筛选控件可建立。

当前文档前半部分保留旧版 `run_desktop.py` / `prototype.py` 的回归测试；新版 `software_app/` 的需求和开发顺序以 `功能路线图.md`、`开发与架构说明.md` 为准。

## 2026-09-13 PixivUtil2 ZIP 行为迁移为公共后处理插件

- 澄清两类“插件”：设置页原有列表是平台 `CrawlerAdapter`；用户所指的是 `历史资料/原始需求.txt` 中要求参考 Pixiv 压缩功能并让所有平台使用的后处理。平台列表现改名为“平台接入插件（与上方压缩后处理不同）”，避免再次混淆。
- 参考旧 `PixivDownloadHandler.py` / `PixivHelper.py` 的 ZIP 校验、ugoira 解压和转换成功后清理思路，新建 `software_app/core/archive_service.py`。没有直接运行旧 PixivUtil2 菜单、旧数据库或配置，而是把行为改造成平台无关、进程内、可取消的任务后处理。
- 调试版支持普通 ZIP 安全解压、按整个任务压缩、按资源文件夹压缩，以及校验成功后可选清理精确源文件。默认全部关闭且默认保留源文件；首次开启清理会再次确认。压缩采用临时文件、ZIP64、成员数与 CRC 校验、原子替换；取消发生在清理开始前会保留源文件和移除临时包。
- 安全解压拒绝路径穿越、绝对/驱动器路径、符号链接、Windows 非法或保留名称、加密成员、超过 10000 个文件、解压后超过 2 GiB，以及超过阈值的高压缩比大文件。先写临时目录，完整成功后才改名为正式目录。ugoira ZIP 被排除，继续交给 Pixiv 专用动画处理器。
- `TaskManager` 在平台下载完成、最终完成状态之前执行公共后处理；新增压缩包和解压文件进入统一 `FileRecord`，清理掉的源文件同步删除 SQLite 索引。所有桌面新任务、候选队列、Google 页面/Pixiv 转交和批量重试均读取同一组设置。
- 新增 9 项归档相关回归，覆盖中日文/空格路径、ZIP 完整性、成功后清理、取消无残留、路径穿越拒绝、安全解压提交、显式上报与回退扫描两类 Adapter、清理后索引一致性，以及设置向所有平台任务传递。完整离线回归共 147 项通过，`compileall` 通过；设置页在 `1080x680`、`1240x780`、`1500x860` 下确认可滚动且公共归档卡片无越界。

## 2026-09-13 Pixiv 收藏、小说文本、网络诊断与响应式操作区

- 本次重新查看的 `1.txt` 是 6 行运行日志，连续两次停在“正在请求 Pixiv 关注第 1 页；服务器尚未返回，本次已取得 0 个”，不是作者页 HTML 或分页 JSON。进一步只读诊断确认：软件当前保存的显式代理无法连接；关闭环境代理继承后的真实直连同样在连接阶段超时。因此这次现象属于网络链路未建立，不能声明真实头像或关注分页已经在线修复。设置页新增“检查 Pixiv 连接/头像”，关注日志会说明当前使用设置代理还是直接连接，代理不可达时要求修改或清空设置，但不会显示代理地址或 Cookie 值。
- `PixivCrawler` 现在设置 `Session.trust_env=False`，避免用户在软件里清空代理后仍被环境变量悄悄送到旧代理。作者 AJAX、关注分页、FANBOX/Sketch 受限会话和 `pximg` 头像均沿用明确的软件代理边界；头像由 Adapter 校验 Pixiv/pximg HTTPS 域名、Referer、Content-Type 和 5 MiB 上限后交回界面。
- “作品收藏 / 小说收藏”改为当前 Cookie 账号的数据源。更新按钮显示“更新我的作品收藏 / 小说收藏”，忽略容易误导的手动搜索输入，从 `PHPSESSID` 安全识别账号；本地结果分别原子写入 `data/software_app/json/pixiv/bookmarked_works.json` 和 `bookmarked_novels.json`，切换范围或点击“读取本地收藏”可离线恢复。无法识别账号时明确要求重新导入 PHPSESSID。
- Pixiv 小说每个目标现在同时生成可阅读 HTML、带 UTF-8 BOM 的 TXT 和原始 JSON，并为三者写入 `FileRecord`。TXT 包含标题、作者、发布时间、标签、来源 URL 和正文，中文 Windows 文本编辑器可直接打开。
- 候选页操作区按“更新 / 单项 / 范围说明 / 选择状态 / 批量 / 关注进度”分行。进度仅在 Twitter“关注账号”和 Pixiv“关注画师”显示；作者/作品候选及收藏不再被进度条横向推移。左侧外链选择与操作按钮拆行，插件详情改为多行“模块 / 用途 / 说明”，FANBOX 文案统一使用“订阅 / 付费内容”，避免含糊的“支持 xxx”。
- 新增小说三格式、本人收藏缓存、插件用途、显式代理会话、Pixiv 超时说明、头像来源/响应类型校验和代理错误提示回归。`python -m unittest discover -s tests -q` 共 138 项通过，`python -m compileall -q software_app` 通过。隐藏窗口在 `1080x680`、`1240x780`、`1500x860` 下逐一切换作者搜索、作品收藏、小说收藏和关注画师，候选操作与左侧外链按钮均未越界，进度条显示范围符合预期。

## 2026-09-13 Pixiv 扩展、平台插件与主题解耦

- 新建 `data/software_app/json/<platform>/` 作为按平台组织的 JSON 运行数据目录；Pixiv Cookie 和后续关注缓存使用 `json/pixiv/`，不再把新的结构化文件散放到软件数据根目录。
- 本机 Pixiv 与 FANBOX 会话已导入并通过字段读取、模块验证和 Git 忽略检查；未在日志中输出值。发现 Windows 已配置无凭据的本机系统代理后，将其同步到软件代理设置并完成在线只读验证：Pixiv 账号状态、公开/私密关注 AJAX 均成功，FANBOX 登录专用支持方案接口返回有效 `plans` 结构。
- Pixiv 真实全量关注测试显示站点声明总数 260，但公开分页实际只返回 259 个有效用户，私密关注为 0。第 2 页原始数组本身仅 47 条；匿名比较 offset 95/96 后确认 offset 95 会重复上一页尾项，固定 48 偏移正确，缺项属于站点计数中仍保留但接口不再返回的不可见/失效账号。结果按 `incomplete_total` 保存 259 项并保留旧缓存删除保护，不能为了凑总数错误推进偏移。
- Pixiv ugoira 新增 ZIP/帧清单校验、解压总量与帧数上限、取消检查，并使用每帧真实 delay 生成 GIF；全局选择 MP4 时再交给 FFmpeg 转码。不会再把 ugoira 当普通 `/pages` 静态插画下载。
- 漫画系列、小说 ID、作者小说、小说搜索、小说系列、排行榜、新作、作品收藏、作者+标签、FANBOX 和 Sketch 从“规划”改为可用入口。小说现保存 UTF-8 BOM TXT、HTML 和原始 JSON；排行支持模式/作品类型/日期输入，标签搜索支持开始/结束日期、最低收藏数和 all/safe/r18，收藏保留收藏 ID/时间字段并可按返回时间过滤。
- Pixiv 设置新增公开/私密/both 范围、日期、最低收藏数和年龄分区。关注更新可读取公开、私密或合并两者，并显示分页进度；重试任务会采用当前 Pixiv 上限、AI、范围、日期、收藏数和年龄设置。Cookie 导入接受 PHPSESSID 或 FANBOXSESSID，且 FANBOX/Sketch 请求使用受限会话，不把 Pixiv Cookie 发送到其他域名。
- 增加统一平台插件加载器。七个内置平台/功能模块均通过插件描述注册；外部插件要求本地 `plugin.json`、API 版本和显式 `enabled=true`，重复模块 ID 默认拒绝，替换内置模块必须显式 `replaces`。设置页显示加载状态，CLI 可用 `python .\run_software.py plugins` 检查。
- 设置页的平台插件区新增明细表、来源/状态/接管关系、外部插件启停、安全确认、打开目录和开发说明入口。插件开关只修改清单并要求重启，不在任务运行期间热换 Adapter；外部 Twitter 接管成功时，内置 Twitter 明确显示“已被接管”。
- 视觉样式抽到 `software_app/ui/theme.py`，调整为中性工作区、深蓝侧栏和靛蓝主色。页面布局与事件接口不变，因此以后继续美化或替换 Tk 视图不会牵动下载器和插件。
- 离线扩展测试覆盖 ugoira delay、路径安全、漫画/小说系列、排行、新作、收藏、作者标签、小说文件、FANBOX/Sketch 响应、插件显式启用/停用/Twitter 接管、GIF 类型、在线预览筛选参数和公开/私密关注的完整及中断合并。当前完整回归 117 项通过；三种窗口尺寸可初始化。Pixiv/FANBOX 会话与关注接口已在线验收；R18、付费帖媒体和真实 ugoira 下载仍需对具体目标单独验收。

## 2026-09-13 相似搜索进度说明与 Pixiv 完整性复查

- 相似搜索结果区原来只有进度条，含义容易与任务下载进度混淆，现明确标为“推荐搜索进度”。单图搜索时表示连接 Google、搜索和整理候选的阶段；文件夹批量搜索时表示“已处理查询图片数 / 查询图片总数”。它不表示候选网页中的图片下载进度，实际下载进度仍在“任务队列”查看。
- Pixiv 作品/作者下载补齐主动取消检查和逐作品、逐页面进度上报；取消后会进入统一的已取消收尾，不再继续处理后面的页面。搜索结果从固定首屏 20 项改为按需要翻页获取，单次任务上限可在 1–100 间设置。
- Pixiv 作者在线资料会把 `commentHtml` 清理为可显示的纯文本简介，并保留主页及社交外链；下载入库记录补齐真实作品标题、页码和作品 ID 元数据。设置页新增“单任务最多作品”和“跳过明确标记的 AI 作品”，新建的 Pixiv 工作台/预选任务会携带这两个选项。
- 修复关注账号的危险默认值：未填写用户 ID 时不再偷偷使用示例账号 `53292267`。现在依次采用输入框、已保存的 `pixiv_following_user_id`、当前 `PHPSESSID` 中可安全识别的账号 ID；都没有时明确提示。Pixiv 关注缓存还可导出 UTF-8 JSON 或 UTF-8 BOM CSV。
- Pixiv“更新新的”现在从最新分页向后扫描，稳定命中旧缓存中的 24 个用户 ID 后提前结束并保留未扫描的旧记录；“更新全部”仍需完整遍历后才确认取关。此阶段之后已经在设置页接入公开、私密和两者合并范围；两者合并时任一侧中断都会保留旧缓存，不误判取关。
- 当前 `1.txt` 是 Pixiv 用户资料页源码，不包含 `/ajax/user/{id}/following` 的实际分页响应，因此只能验证用户资料结构，不能证明关注列表接口成功。当前机器没有 `data/software_app/pixiv/cookies.json`；沙箱代理不可用，授权直连 `www.pixiv.net:443` 又在连接阶段超时，所以本轮没有把 Cookie、R18、私密关注或真实下载标为在线通过。
- 本次复查时发现的 ugoira、小说/系列、排行、新作、作品收藏、日期筛选、FANBOX 和 Sketch 缺口已由上方“Pixiv 扩展”阶段补齐；仍待外部条件的是带有效 Cookie 的真实账号在线回归。
- `compileall` 和完整离线回归共 106 项全部通过，新增覆盖分页搜索、增量关注重叠停止、预取消、下载进度/文件元数据、作者简介与外链、Pixiv 设置传递，以及关注 JSON/CSV 导出。

## 2026-09-13 下载库预览与推荐搜索进度

- 下载库“图片预览”标题栏增加加载状态和确定进度条，后台依次反馈读取文件、读取图片信息、校正方向、生成缩略图和显示预览；成功显示 100%，非图片、无图片或失败会明确复位并显示原因。
- 修复预览控件显示图片后再次触发自身尺寸事件、不断重新读取同一图片的问题。现在监听稳定的外层预览区域，并用规范化路径、32 像素尺寸档位、文件修改时间和文件大小组成请求键；同一文件版本和同一尺寸只生成一次，切换图片、文件发生变化或窗口尺寸跨档位时才重新生成。旧线程的进度和结果继续通过 generation 校验丢弃，不会覆盖当前选择。
- Google 相似图片候选/推荐区域增加确定进度条。开始连接、逐张获取推荐、单张完成以及全部完成都会更新进度；批量中止或部分失败保留已完成比例，真正失败复位为 0。
- 新增同图同尺寸预览去重回归；使用真实临时 PNG 在隐藏 Tk 窗口中确认进度到达 100%、图片成功显示，随后重复请求没有再次排队。

## 2026-09-13 界面截图报错与图片目录约定

- 查看 `图片/1.png`，确认“预览失败：`SoftwareDesktop._selected_types() missing 1 required positional argument: 'self'`”来自界面拆分时遗留的两个 `@staticmethod` 装饰器；`_selected_types` 被错误变成静态方法。已移除遗留装饰器，预览和任务参数读取可再次通过实例调用。
- 扫描 `software_app/ui` 下全部类方法，除截图对应问题外还发现 Google 页的 `_google_source_missing` 和 `_download_module_for_target` 在拆分时丢失 `@staticmethod`，通过窗口实例调用会发生参数错位，现已一并补回。新增 `_selected_types()` 实例调用、静态方法禁止引用 `self`、普通实例方法首参数必须是 `self` 三类回归，防止以后拆分方法时再次遗留或丢失装饰器。
- 后续软件界面截图、报错截图、待分析图片和图片功能测试素材统一放在 `图片/` 目录，并以该目录中的原图作为图片类问题的检查入口；具体约定见 `图片/README.md`。

## 2026-09-13 桌面界面按页面拆分

- 将原本接近 5000 行、同时承担所有页面构建和操作的 `software_app/ui/tk_app.py` 按界面领域拆分。主窗口现在通过轻量 Mixin 组合页面能力，保留窗口生命周期、平台工作台、跨页面任务事件和统一状态协调。
- `ui/library_tab.py` 负责下载库搜索、后台索引/扫描、文件批量操作和异步预览；`ui/google_search_tab.py` 负责相似图片查询、候选检查/导出/下载；`ui/settings_tab.py` 负责可滚动设置页、格式持久化与浏览器驱动检查；`ui/task_queue_tab.py` 负责任务列表、详情、批量重试/取消/删除和输出目录操作；`ui/following_tab.py` 负责各平台关注/候选、历史、导入导出及 Twitter/Pixiv 关注更新。
- `tk_app.py` 从 4865 行降至 1987 行；下载库、Google、设置、任务队列、关注/历史五个页面模块分别为 621、699、266、390、1082 行。模块只依赖核心服务和共享 UI 工具，不反向导入主窗口，避免循环导入；原有入口及测试需要的兼容导出继续保留。
- 对拆分后的所有模块执行语法编译和隐藏 Tk 窗口实例化；设置滚动容器、任务多选表均正常创建。完整离线回归 96 项全部通过。

## 2026-09-13 设置滚动、下载库搜索与路径字符兼容

- “设置”页改为 Canvas + 垂直滚动条，鼠标位于设置内容区时可用滚轮；标题保持可见，小窗口可以滚到驱动检测和底部说明，不再因窗口高度看不到最后几项。
- “下载库”增加即时搜索和清除入口。搜索索引包含平台、文件夹、完整路径、文件名、媒体类型、标题、作者名/ID、来源 ID、章节和标签；文本先做 Unicode NFKC 与 `casefold`，全角/半角和大小写可统一匹配，空格分开的关键词采用 AND 组合。采用预计算搜索键、250ms 输入防抖和线性子串匹配，避免对每次按键运行高成本编辑距离或模糊算法。
- 目录扫描从界面线程移到后台线程，界面可继续使用，并每 500 个媒体文件反馈一次数量。数据库由“每文件一次连接/事务”改成单连接批量写入；扫描用 `os.walk` 跳过构建目录和符号链接。下载库索引只读取搜索需要的列，最多加载 100000 条并在达到上限时明确提示。
- 文件夹列表最多渲染 2000 行，每个文件夹的文件表最多渲染最新 500 行，超过时提示继续搜索缩小范围。图片点击/自动预览增加 120ms 防抖并在后台解码、按控件尺寸生成缩略图；快速切换时旧结果不会覆盖当前图片，因此大图或图片很多时不再阻塞 Tk 主线程。
- 文件名统一做 NFC 保存规范化、控制字符/Windows 非法字符替换、尾部句点空格处理、UTF-16 长度截断，并保护 `CON`、`NUL`、`COM1`、`LPT1` 等 Windows 设备名；Twitter 主页缓存、作者目录及媒体名也补齐保留名处理。下载库重命名会在操作前解释非法字符，设置保存目录会拒绝空字符/无效 Unicode，并对超过 180 字符的 Windows 根目录提示后续嵌套目录及 FFmpeg 长路径风险。
- SQLite 使用 Unicode TEXT，JSON 继续使用 UTF-8/`ensure_ascii=False`，CSV 使用带 BOM 的 UTF-8，Windows 回收站调用使用宽字符 API。新增 Unicode 搜索、组合/预组字符、Emoji UTF-16 长度、设备名、批量扫描进度和任务归属保持回归；语法检查、隐藏窗口组件/500 行上限检查及完整离线回归 96 项通过。

## 2026-09-13 关注本地缓存提示与安全文件更新

- “关注 / 用户”中的“读取本地”现在会弹出读取结果，显示平台、有效账号数、缓存文件绝对路径、完整/部分状态、最近更新方式以及在线采集/文件导入时间。文件不存在、JSON 损坏或结构错误时会明确提示没有使用其中账号；读取本地不会联网或自动下载。Pixiv“关注画师”的本地读取也使用同样提示。
- Twitter 的“导入关注”改名为“从文件更新”。JSON、CSV、TXT/LIST 都先只读预检，展示原始数、有效去重数、新增、同名更新、无效条数和样例，用户再次确认后才写盘。Twitter 用户名严格按 1–15 位 ASCII 字母、数字和下划线校验；异平台 JSON、普通设置 JSON、缺少账号列的 CSV 和零有效账号文件直接拒绝，原缓存保持字节不变。
- 文件更新是合并操作，不会根据导入文件删除旧账号；写入前自动生成 `following_accounts.pre-import-*.bak`，再用临时文件原子替换。导入后的缓存标为 `partial=true` / `update_mode=file_merge`，因为离线文件不能证明它是当前完整关注集合；只有成功“更新全部”才能核对并移除已取关或误导入账号。
- 离线校验无法确认一个格式合法的用户名是否属于预期对象。此类账号在候选表标为“文件导入”；真正加入下载队列时会二次警告。不存在的用户名通常会让任务失败；若错字恰好对应另一个真实账号，则可能下载到错误账号内容，因此建议先“在线获取资料”核对。
- 新增错误平台、普通配置、全无效账号、预检不写盘、自动备份和 Twitter/Pixiv 缓存状态回归；`py_compile` 与完整离线回归 93 项全部通过。

## 2026-09-13 全局统一输出格式与设置复查

- “设置”页新增统一输出格式：图片可选 PNG、JPEG、WebP、保留原格式；视频可选 MP4、保留原格式；动图可选 GIF、MP4；音频可选 MP3、M4A。默认仍是此前约定的 PNG / MP4 / GIF / MP3。
- 格式设置保存到新版 SQLite settings 表。没有保存记录时加载上述默认值；点击“保存设置”后与保存目录、代理、失败重试、Google 候选数/等待时间、网页最多文件和网页读取超时一起持久化，重启后恢复。
- 新建的普通平台任务、左侧预选队列任务和相似图片网页任务都会携带当前格式。批量重试不再原样沿用旧任务的格式，而是保留原目标/目录/抓取参数，并用当前设置覆盖四类输出格式；旧版零文件相似图片任务同样适用。
- 网页候选根据真实 Content-Type 和媒体种类选择输出扩展名，图片通过 Pillow 解码重编码，视频、动图、音频通过 FFmpeg 转码，不会只改文件后缀。Twitter 图片不再固定 PNG，GIF/音频兼容参数由新设置同步生成；Pixiv 和 JMComic 图片也接入统一图片格式。
- 统一格式只影响之后新下载或重试后新产生的文件，不批量改写磁盘上已有文件。选择视频/音频/动图转码但系统缺少 FFmpeg 时，任务会明确失败并保留错误信息。
- 隐藏桌面使用临时数据库验证 JPEG / MP4 / GIF / M4A 能保存并进入新任务参数；图片透明通道转 JPEG 会使用白色背景。三个 Twitter 配置 JSON 均通过语法检查，`compileall` 和完整离线回归 90 项通过。

## 2026-09-13 相似候选详情页、任务取消与统一媒体格式

- 相似图片候选不再根据域名转调 Twitter、Pixiv 等平台下载器；全部作为网页详情任务，由可见浏览器直接打开候选 URL，再从渲染后的页面提取媒体。旧版错误路由的候选任务在批量重试时自动迁移为网页详情任务。
- X 单帖会用现有 Cookie 打开详情页，并限定到对应帖子正文，只保留 `pbs.twimg.com/media/` 图片，排除头像和回复；CDN 地址统一规范为带真实扩展名的 `name=orig`。`1.txt` 中该帖正文的 `tweetPhoto`、`/photo/1` 和扩展插入的原图链接都指向 `HPA4rIRa8AAyW0z.jpg?name=orig`。
- 帖子 `2085248253013180813` 的旧任务实际发现了 1 张图，但独立 HTTP 下载遇到 `ConnectionResetError(10054)` 后被旧逻辑当成跳过，并误记成零文件完成。现在单资源默认重试 3 次；全部资源失败或浏览器没有发现媒体时任务明确标为失败，不再假完成。旧版“相似图片网页任务 + 零文件完成”可在任务队列中重新批量重试，普通已完成任务仍不能重复执行。
- 同一帖子真实验证时，`pbs.twimg.com` 连续 3 次重置 Python HTTP 连接，而 Chrome 内正常显示。因此 X 详情页会优先由已经打开页面的 Chrome 读取正文媒体，再交给统一保存/格式处理逻辑，不会按域名切换到 Twitter 下载模块。实测已保存 `001-HPA4rIRa8AAyW0z.jpg`，JPEG 1120×1440、171703 字节，原地址为 `name=orig`。
- 取消不再在界面线程同步等待浏览器退出；任务结束会先落库并从活动表移除，即使结束回调异常也不会留下幽灵任务。Twitter 滚动、重试、下载队列和媒体写入增加取消检查；批量重试去重相同的活动目标。
- 媒体输出规则默认统一为视频 MP4、音频 MP3、动图 GIF，并可在全局设置中调整；Twitter 默认开启 GIF 转换、不保留动图源 MP4，音频默认改为 MP3。格式转换需要本机 FFmpeg，并完成上述单帖真实下载验证。

## 2026-09-13 相似图片候选失效处理

- 相似图片候选表新增“状态”列，区分待检查、可访问、链接已失效、登录/访问受限、网站暂时异常和源图不存在。404/410 才自动判定为失效；401/403/407/429 只标记为受限，网络错误标记为无法确认，避免把登录、反爬和临时断网误当成页面删除。
- “打开网页”仍立即交给系统浏览器，不重新变成阻塞式网页预览；打开成功后会在后台检查该候选。用户也可多选后点击“检查链接”，检查期间按钮可用于停止，并在退出软件时收到取消事件。
- 部分站点（尤其客户端渲染页面）会以 HTTP 200 显示“内容不存在”，普通 HTTP 检查无法可靠判断。为此新增“标记失效/恢复待检查”：用户在浏览器确认内容已删除后可以手动标记。失效行以红色显示，下载时会被跳过；仍保留在列表中供核对、恢复和导出。
- 原查询图片被移动、改名或删除时，预览区明确显示原路径并禁用重新搜索，旧候选网页仍可继续打开。批量图片中只有部分路径失效时会记录并跳过失效图片，继续处理剩余图片，不让整批失败。
- CSV/JSON 候选导出新增源图片完整路径、可用状态和 HTTP 状态码，之后重新载入或人工核对时能知道当时为何没有下载。
- 新增 HTTP 200/403/404/410/429/503 状态分类回归；完整离线测试 72 项全部通过，隐藏桌面窗口确认状态列、源图不存在提示、手动标记和失效项禁止下载正常。

## 2026-09-13 Pixiv 分页关注画师

- 早期样本曾用于设计分页 HTML 回退解析；当前更新后的 `1.txt` 是用户资料页源码，不包含可直接解析的关注分页列表。关注画师的真实完整性必须以 AJAX 响应或实际 `following?p=N` 页面为准，不能由当前文件推断。
- 新增 `software_app/crawlers/pixiv/following.py`。在线刷新优先调用 Pixiv 原生分页接口 `/ajax/user/{id}/following?offset=...&limit=48&rest=show`；接口不可用时才回退到普通 `?p=N` HTML 页。HTML 可能只含服务端首屏子集，因此会对照页面总数；数量不足的结果标为部分结果，绝不用于删除旧缓存。
- Pixiv 候选页新增可用的“关注画师”种类、“更新新的”和“更新全部”。两者都会真实遍历分页并按稳定用户 ID 去重：更新新的采用当前分页顺序更新资料、合并新画师并保留缓存中暂时未返回的账号；更新全部只有在确认分页完整时才替换缓存并统计取关。取消、后续页失败、页数保护或页面子集都会保留已取得结果和旧缓存。
- 关注缓存写入 `data/software_app/pixiv/following_accounts.json`，包含用户 ID、名称、简介、头像、主页、公开状态、账号和完整/部分状态。候选表不再截断 100 条，刷新会保留已有选择与滚动位置；左侧目标区同步显示所有已缓存画师，并可多选加入现有 Pixiv 两路并发下载队列。
- “关注画师”页会把按钮切换为“导入 Cookie”。支持浏览器导出的 JSON 数组或名称/值对象，要求包含 `PHPSESSID`，规范化后保存到 `data/software_app/pixiv/cookies.json`，不会显示或记录 Cookie 值。公开关注会在无 Cookie 时尝试，非公开关注仍要求有效登录 Cookie。
- Pixiv 作者在线预览补充头像 URL 和主页 URL，获取到资料后可显示头像。当前机器仍未配置 Pixiv Cookie，所以只完成 `1.txt` 页面结构验证、模拟 AJAX/分页/部分结果验证和隐藏桌面界面验证，没有冒充真实账号在线成功。
- 完整离线回归共 71 项通过；新增覆盖 AJAX 字段、稳定 HTML 属性、页码/总数、新增合并、全量取关、不完整 HTML 禁止删除缓存及浏览器 Cookie 导入。

## 2026-09-13 相似图片候选打开、下载路由与超时

- 最近三条相似图片下载失败均为 Google 候选中的 `x.com/.../status/...`，旧运行实例把它们交给通用网页爬虫并在 `read timeout=10` 处失败；不是 Google 相似搜索的人机验证等待过短。
- “预览网页”改为“打开网页”：单击、双击或回车直接用系统浏览器打开候选，不再为了预览先请求并解析页面，因此不会再在预览阶段触发网页读取超时。
- 此处旧版曾按域名把候选转给平台下载器，现已取消：相似搜索得到的是具体详情页，必须保持网页候选语义，用浏览器打开该页并直接提取页面媒体。
- 普通网页超时按反馈下调为适中值：HTML 连接/读取默认 20/60 秒，媒体连接/读取默认 20/120 秒；设置页仍可在 30–600 秒范围调整网页读取时间，避免慢站受旧版 10 秒限制，同时不长期占用并发槽。
- 开始拆分 3988 行的桌面主文件：平台/状态/搜索种类配置迁到 `ui/platform_config.py`，文件批量扫描、回收站调用、整数边界及下载域名路由迁到 `ui/desktop_support.py`；`tk_app.py` 保留兼容导入，现有测试和入口无需改写。后续按界面领域继续拆任务、历史、Google 与本地库控制器。
- 开始后续 Pixiv 开发时先修复在线搜索预览引用未定义 options 的问题；Google 发现的 Pixiv 作品链接已可直接交给现有 Pixiv 原生下载器。由于本机尚无 `data/software_app/pixiv/cookies.json`，登录限定的关注/收藏功能不能冒充已完成真实验证。
- `compileall`、网页下载超时/域名路由专项与完整离线回归 66 项全部通过；隐藏界面确认“打开网页”和默认 120 秒设置正常加载。

## 2026-09-13 批量任务、历史清理与退出收尾

- 任务队列改为 Ctrl/Shift 多选，支持批量重试、批量取消和删除任务记录。删除已结束任务会精确移除对应任务、日志及文件索引；删除运行中任务会先显示“正在取消”，让 adapter 关闭浏览器/网络任务，收到结束事件后再删记录。两种情况都保留已经下载到磁盘的文件。
- 取消事件现在先持久化为 `cancelling`，即使浏览器在取消过程中抛出窗口已关闭错误，最终也记录为“已取消”而不是“失败”。任务表在有活动任务时每秒从 SQLite 同步一次，其他平台历史中的英文状态也改为中文；异常退出遗留的 queued/running/cancelling 记录会在下次启动恢复成已取消。
- 点击窗口关闭且仍有排队/下载任务时会询问用户：确认后统一设置取消事件、关闭 adapter 并最多等待 8 秒，已下载文件保留；取消关闭则软件继续运行。无法及时结束的任务会先标记为取消，避免下次启动仍显示假运行。
- 各平台历史表支持多选“清理选中”和“清理本平台全部”。Twitter 会同时清理相应 downloaded JSON 与 SQLite 索引，并用隐藏记录避免仅因本地作者文件夹仍存在而马上重新出现；新的同作者下载会自动解除隐藏。运行中的历史任务同样先停止再清理。
- 左侧“显示搜索/关注/历史”列表增加多选和“加入选中到下载队列”，搜索结果不再只能预览。它与候选页共用同一任务创建和平台并发限制，Google 页面候选会转交网页资源模块下载。
- `compileall`、隐藏窗口组件检查和完整离线回归 64 项通过；新增覆盖取消期间 adapter 抛错仍记为取消、启动恢复中断状态、精确删除任务索引但保留媒体、Twitter 选中历史清理后本地文件仍在且列表不回弹。本机数据库中 4 条没有 Python/ChromeDriver 执行进程、停留约 28 分钟的旧 `running` 记录已恢复为“已取消”，未删除其文件或任务记录。

## 2026-09-13 Skeb Button、头像缓存与候选下载队列

- `1.txt` 已确认来自 Skeb 官方 Chrome 扩展 “Skeb Button”，不是 X 原生简介节点。新版解析器除 `UserDescription`/`UserProfileHeader_Items` 外，也只在独立 `.skeb` 组件内接受无外层容器的链接，仍排除页面其他区域的任意外链。
- Twitter 自动化驱动会在 Chrome/Edge 各配置目录中查找扩展 ID `onjegdbehgoamaiochjfnkokondpgoim`，验证 manifest 名称和 `index.js` 后，以只读 unpacked extension 方式加载到每次独立临时资料会话。主页用户名出现后会等待官方扩展插入 `.skeb a[href*='skeb.jp/@']`，再写入预览缓存；未安装扩展时安静降级，不自行伪造 Skeb 地址，也不绕过网页直接请求扩展内部 API。
- 本机 `Profile 1` 找到官方扩展 1.10。真实刷新 `@1100_2299` 后得到唯一账号链接 `https://skeb.jp/@1100_2299` 及“停止中（納品実績: 55件）”；Skeb 根地址仍可保留在普通外链，但不会再作为重复的账号 Skeb 项。
- 关注列表已有 `avatar_url` 时，本地资料预览会在后台直接缓存 `pbs.twimg.com` 官方头像，不再要求用户点击“在线获取资料”来启动完整主页浏览器。只允许 HTTPS 官方头像域名、图片响应和最大 5 MB，并复用 Twitter 媒体下载器已有的系统代理、显式代理与重试设置；成功后写入本地头像缓存，之后完全离线显示。
- 候选表改为 `extended` 多选，支持 Ctrl/Shift、“全选当前页”和“取消选择”。“下载选中”改为“加入下载队列”：一次把所有预选目标建立为正式任务并切到任务队列；任务管理器按各 adapter 的信号量并发，Twitter 5 路、JMComic 3 路、Pixiv 2 路、网页 3 路，超过上限的任务保持 queued 等待，而不是无限创建浏览器。
- 隐藏界面用伪任务预选 3 个 Twitter 作者，确认 3 个目标全部进入队列、状态显示“Twitter/X 5 路”、未发起真实下载。本地资料回读确认 `@1100_2299` 无需再联网即可显示已缓存 Skeb，关注账号 `@nig2ru` 已从关注资料直接得到简介、2 条外链及本地头像路径。
- `compileall` 与完整离线回归 60 项全部通过；新增覆盖独立 `.skeb` DOM、跨 Chrome profile 查找官方扩展、头像 CDN 域名/大小限制与缓存复用。

## 2026-09-13 Twitter 资料补全与左侧选择状态

- 左侧“已抓取/历史中查找”结果改为稳定选择：点击后保留表格高亮，并在列表上方显示“已选中 · 来源 · 名称”；不会因为再次选择同一平台而重建列表，也不会自动刷新或跳回顶部。确实需要重建列表时，会按目标键恢复选择和原滚动位置；只有新的搜索结果替换旧结果时才清除旧选择。
- 顶部和关注/历史页的按钮统一区分为“本地预览”和“在线获取资料”。本地预览只读主页缓存、关注缓存和下载历史，不再为了头像发起网络请求；在线操作才访问主页并更新头像、简介、Skeb 和其他外链。
- 左侧简介改为固定 4 行高、带垂直滚动条的只读区域，长简介不会继续撑高并遮住主页和外链操作。
- 根据 `1.txt` 的真实主页 DOM，外链解析范围补到 `UserDescription` 和 `UserProfileHeader_Items`，能从显示地址恢复 FANBOX、Skeb、lit.link 等展开链接，同时排除页面导航里的无关链接。关注列表则同时从 GraphQL `legacy.entities` 和每个懒加载 `UserCell` 的可见链接提取外链。
- 关注缓存现在携带头像、简介、外链和 Skeb；下载历史会用关注缓存和主页缓存补齐这些字段。旧记录只有作者文件夹名时，仅在该显示名能唯一对应到一个已知 handle 时合并，名称有歧义就不猜。开始 Twitter 下载时，会复用同一个已登录浏览器先尝试更新作者主页资料，再抓取媒体；资料抓取失败只给出提示，不阻断媒体下载。这样关注、历史、下载三个入口都能逐步积累可本地预览的资料。
- Twitter Chrome 会话使用软件目录内的独立临时用户资料，避免默认 Chrome 资料锁冲突；正常退出和启动失败都会清理临时目录。
- 再次真实执行“更新全部”，由页面到底且连续 15 轮无增长自动结束，最终保存 1165 个账号、`partial=false`、`completion_reason=list_end`；其中 1150 个有头像、1118 个有简介、537 个账号有外链（共 873 条），176 个账号有 Skeb 链接。
- 最终 `compileall` 和完整离线回归 57 项通过；隐藏桌面窗口加载 1167 个左侧目标，抽查第 501 项点击后仍保持高亮和滚动位置，简介控件高度为 4。`1.txt` 离线解析得到 FANBOX、Skeb 根地址、Skeb 用户地址和 lit.link 四条链接。

### 其他平台同类功能记录（本轮未实现）

- Pixiv：界面同样提供“更新新的/更新全部”。增量按稳定用户 ID 去重，从最新可见页向后扫描到足够旧 ID 重叠；全量遍历完整关注分页，以本次 ID 集合替换缓存并确认取消关注。接入前要用真实账号确认关注列表接口及排序，而不能只依赖昵称或当前行号。
- Bluesky：界面同样提供两个更新入口，但以 DID 和游标分页为准。增量通过 DID 集合与旧快照重叠停止，全量走完游标后才允许删除旧记录；不把客户端当前显示顺序当成永久排序保证。
- Instagram：关注列表顺序可能受推荐、互动和账号状态影响，先明确记录为“不假设新到旧”。“更新新的”只能按稳定账号 ID 做集合增补，不能凭顶部位置判断取关；“更新全部”必须完成登录后的完整分页后才计算新增/取关。若平台只能返回不完整或受限列表，应标记部分结果且禁止删除旧缓存。

## 2026-09-13 Twitter 关注增量与全量更新

- Twitter 关注页新增“更新新的”和“更新全部”。增量模式从新到旧扫描，以 handle 是否存在和连续 30 个旧账号重叠作为停止条件，不使用会因取关而整体偏移的列表位置；只把新账号插到顶部并保留旧缓存。全量模式滚动到底后替换缓存，可准确移除已取关账号。
- 关注缓存升级到版本 2，不再按用户名排序，保留 X 页面从新到旧的发现顺序；同时记录 `update_mode`、`previous_count`、`new_count`、`completion_reason` 和全量完成时的 `removed_count`。首次没有旧缓存时，增量请求自动退化为全量同步。
- 真实全量回归得到 1165 个账号，新增 0、移除 0，顶部账号顺序与 X 页面一致；随后真实增量回归在首批连续匹配 52 个旧账号后自动结束，新增 0、总数仍为 1165、顶部顺序不变。
- 新增位置偏移、增量合并、旧账号重叠停止、全量替换和页面顺序回归。完整离线测试 53 项全部通过；桌面隐藏窗口在 `1080x680`、`1240x780`、`1500x900` 下确认两个更新按钮可用，并渲染 1165 条左侧关注目标。
- 本轮按要求只完成 Twitter。Pixiv、Bluesky 的同类增量/全量同步，以及 Instagram 关注关系来源和排序规则仍留待后续单独设计与接入。

## 2026-09-13 Twitter 懒加载关注列表修复

- 关注列表采集同时读取 X 虚拟列表 DOM 和浏览器收到的 Following GraphQL 数据，不再只依赖当前屏幕上少量 `UserCell`；首批内容尚未出现时也会主动滚动触发懒加载。
- 采集到的新账号会立即写入部分缓存并推送给桌面界面，左侧目标浏览区和“推特关注”表会在脚本仍运行时持续更新。
- 移除桌面端和命令行的默认 150 秒总时长截断。现在只有“账号数没有新增，并且页面滚动位置/高度也没有推进”连续稳定 15 轮才视为列表到底并自动关闭；用户仍可随时点“停止刷新”保留部分结果，命令行也可显式设置异常兜底时限。
- 修复前的真实刷新在 150 秒处保存了 711 个账号并正确标记为部分结果，证明原问题是时限截断而非完全未获取。修复后同一账号继续真实回归，采集越过原截断点并在页面稳定 15 轮后正常完成，共保存 1165 个账号，`partial=false`，进程退出码 0。
- 新增 GraphQL 解析、首批主动滚动/实时回传、滚动仍推进时不得提前结束等回归；关注采集专项 34 项、完整离线回归 50 项全部通过。桌面隐藏加载验证读取 1165 条缓存，左侧目标区渲染 1165 条关注目标（另含本地历史目标）；关注候选表按产品约束显示前 100 条。

## 2026-09-13 全局设置与浏览器驱动入口

- SQLite 现有 settings 表正式接入桌面“设置”页：默认保存目录、代理 URL、失败重试次数、Google 候选/验证等待和网页文件上限会跨启动恢复，并传给之后的平台搜索、在线预览、下载和候选网页任务。
- 代理仅接受 `http/https/socks5/socks5h` 且必须有主机；界面拒绝保存带用户名或密码的代理，避免凭据进入 SQLite 任务参数、任务详情或日志。
- 设置页新增后台 Chrome/ChromeDriver 检测，以及用户明确触发的“安装缺失驱动”；检测结果包含 Chrome 版本、驱动来源和路径，网络安装失败会回到可操作状态。
- 增加设置类型持久化、整数边界和任务选项脱敏回归。完整离线测试由 45 项增加到 47 项；设置页、关注导入导出和 Google 批量/导出组件在最小窗口请求尺寸下完成无窗口初始化检查。本机 Chrome `122.0.6182.0` 与全局 ChromeDriver 状态检测通过；驱动下载需要网络，本轮没有强制执行。

## 2026-09-13 关注列表导入导出与 Google 文件夹批量

- Twitter 关注候选页新增“导入关注”和“导出关注”。导入支持原生 JSON、CSV、TXT/LIST，按用户名合并并保留旧记录中未被新文件提供的字段；导出支持 JSON/CSV，CSV 对不可信单元格做公式注入防护。
- Google 相似搜索新增递归文件夹选择，单批按稳定顺序读取最多 20 张图片，候选记录查询图片来源并按 URL 去重；全批最多保留 1000 个结果。停止、浏览器关闭或后续图片失败时不会丢弃前面已取得的候选。
- 候选表新增“查询图片”列和“导出候选”，有选择时导出所选、没有选择时导出全部，支持 CSV、JSON 和纯 URL TXT。
- 新增关注列表合并/CSV 回读、候选去重与 CSV 公式防护、文件夹图片类型过滤与数量上限回归。完整离线测试由 42 项增加到 45 项并全部通过。

## 2026-09-13 任务详情与界面重试链路

- 任务队列改为上下可调分栏：上方保留状态表，下方显示所选任务的目标、目录、时间、参数、错误、完整日志和本任务文件，不再需要直接查询 SQLite 才能排查失败原因。
- 排队中或运行中的任务可从详情区单独取消，不影响其他并行任务。失败和已取消任务可从界面重新创建任务；重试读取原任务的平台、原始输入、输出目录和选项，使用新任务 ID 运行并保留旧记录。已完成、排队中和运行中的任务不会误触发重试。
- 增加“填入工作台”和“打开输出目录”，历史任务的下载类型与搜索种类也会恢复到当前输入区。
- 运行中日志按最多每 200ms 的界面轮询批次增量刷新当前行与详情，不再因每条进度消息重建整个任务表；完成时再统一刷新状态与文件计数。
- 任务参数在详情展示前递归脱敏：Cookie、token、密码、密钥以及含账号密码的 URL 不会显示；任务执行和重试仍读取原始持久化参数。
- 修复非 Twitter 平台清理历史后的界面刷新：现在会同步刷新任务、下载库、运行日志和状态提示，仍不删除本地媒体。
- 新增任务详情查询、按任务筛选文件、持久化参数重试及禁止重试已完成任务的回归。`py_compile`、`compileall` 和完整离线测试共 42 项通过；桌面窗口在 `1080x680`、`1240x780`、`1500x900` 三种请求尺寸下完成初始化与组件存在性检查，CLI 模块列表及 Twitter 离线预览通过。

## 2026-09-13 主页资料、关注刷新与相似页面实测

- Twitter 主页头像除了记录 CDN URL，还会截取当前 DOM 中的头像为本地 PNG，左栏优先使用本地文件。实测 `@L0WR15E` 成功生成 `L0WR15E-avatar.png`，并抓到 Skeb 与 fori 两个外链；桌面组件重载后显示头像和“主页外链 2 · Skeb 1”。
- 候选表增加当前选中项提示和“填入上方目标框”；首条候选会自动选中。`@L0WR15E` 精确用户搜索实测可填回顶部目标输入框。
- 关注列表采集改为进程内可停止任务：先等待首批 `UserCell`，再滚动收集；持续回传等待/数量状态并保存部分结果。本机 X 关注页实测未在等待期内返回账号节点，现在温和返回“暂未加载出账号”，不再显示 WebDriver 堆栈，也不用空结果覆盖旧缓存。
- Google Lens 解析同时读取 DOM 和页源码中的转义地址，并排除 Google、`schema.org`、W3C、图片直链和无效主机。保持原有 0–300 秒可调、默认 60 秒的手动验证等待；关闭浏览器按正常结束处理。
- 用 `D:\img_download\QQ图片20260909190459.png` 完成可见浏览器回归，手动验证后实际提取 10 个外部候选，包含 PokemonLore 作品页、Pinterest Pin、WordPress、DeviantArt 等。
- `compileall` 通过，离线回归增加验证页伪链接过滤、具体页优先与关注缓存保留检查，共 39 项通过。

## 2026-09-12 跨平台候选/历史、在线抓取和文件操作

- 重新核对旧 `Pixiv爬虫/PixivUtil2.py` 菜单和 `JMComic爬虫/jmcomic/jm_client_interface.py`，恢复“多种类搜索”的产品结构。Pixiv 搜索种类列出作品/作者 ID、标签全部/插画/漫画、标题简介，以及待迁入的漫画系列、小说/小说系列、排行、新作、收藏、关注画师、FANBOX、Sketch；JMComic 列出漫画/章节目标、综合/作品/作者/标签/角色搜索，以及待迁入的分类排行和收藏夹。
- 此阶段当时只接通 Pixiv 标签的 `type=all|illust_and_ugoira|manga`、标题简介的 `s_mode=s_tc`，以及 JMComic 五种搜索的 `main_tag=0..4`；Pixiv 后续种类现已由本文顶部的新阶段迁入。搜索种类继续写入任务 `options_json`，平台历史显示自己的内容种类。
- Twitter 外链采集优先使用 DOM 的展开 URL/显示 URL，仅在剩余地址为 `t.co` 时解析跳转；简介文本先剔除邮箱，避免把邮箱两段误判为网站。左侧新增独立外链下拉、Skeb 计数、打开/复制外链和复制简介操作。
- 使用现有 Cookie（未输出内容）对公开主页 `@L0WR15E` 真实回归：抓到 `https://skeb.jp/@L0WR15E` 和 `https://fori.io/L0WR15E`，缓存重新载入桌面后显示“主页外链 2 · Skeb 1”。另一个测试主页只写了 Skeb 说明而没有实际 URL，因此没有伪造链接。
- 窗口在 `1080x680`、`1240x780`、`1500x845` 下检查通过。高度不足 740 时隐藏左侧重复的静态 HTML 详情，保留头像、简介和外链操作，使最小窗口的目标列表与日志仍可使用；默认和宽屏继续显示 HTML 详情。
- 根据路线图 3.4、5.1–5.3 修正固定 Twitter 页：候选与历史页签、表头、按钮和数据现在随平台切换；Twitter 保留关注列表，Pixiv/JMComic 复用作品候选，Google/网页资源显示页面候选，各平台历史从自己的任务和文件索引读取。
- Twitter 搜索结果会加入候选页，不再只出现在左侧临时列表。`https://x.com/home` 作为登录首页处理：读取现有 Cookie 后识别当前账号，再进入个人主页抓取动态资料。
- 在线主页采集会等待 `UserName` 动态节点，登录限制或等待超时时给出明确错误；新增独立“打开网页”按钮用于保持系统浏览器页面，“在线”仍负责自动抓取并在完成后关闭自动化窗口。
- 使用现有 14 项 Cookie（未输出内容）在沙箱外真实验证 `https://x.com/home`：成功识别账号、抓取显示名、头像和主页地址并写入缓存；该测试主页未发现 Skeb/其他外链。
- 下载库文件列表改为多选，增加打开、复制路径、重命名和“移到 Windows 回收站”。重命名与回收站操作同步 SQLite 文件索引；清理历史仍不删除本地媒体。
- 修复扫描归属：无任务 ID 的手动扫描不再覆盖已有文件的平台。检查现有下载时发现 11 个文件均存在，但旧扫描曾把它们的 SQLite 平台标记为 JMComic；确认没有 JMComic 任务且文件与 Twitter 记录相符后，已备份数据库到 `content_downloader_v2.pre-platform-fix-20260912.bak`，并把这 11 条索引修正为 Twitter。Twitter JSON/本地目录历史现能正确合并为 2 条，不再把绝对路径盘符 `D:` 当作者名。本轮未删除、移动或改名任何现有下载文件。
- 新增回归覆盖 `x.com/home` 路由、绝对下载路径作者识别、扫描平台归属保持、文件索引重命名/删除、X 展开外链与邮箱排除、Pixiv 漫画搜索参数和 JMComic 作者搜索参数。完整离线测试共 35 项通过。

## 2026-09-12 平台切换、主页预览与相似搜索优化

- 修复点击“在线”时 `sys is not defined`：Twitter 主页/关注采集子进程现在使用当前 Python 解释器正常启动，并增加回归检查。
- 左侧“目标与历史”改为跟随平台：Twitter 显示关注/下载作者，Pixiv、JMComic、Google、网页资源显示各自最近任务和平台说明；选择结果不再强制切回 Twitter。
- 增加平台目标搜索框。Twitter 支持 `@用户名`、用户名或昵称的本地查找；即使没有本地记录，合法的精确 `@用户名` 也会作为直接主页候选，可继续点“在线”抓取简介、头像和外链。Pixiv、JMComic 使用各自内置搜索器。
- 保留独立“相似搜索”页。顶部切换到 Google 会直接进入该页；Google 只负责候选 URL，候选页面预览/爬取会明确切给 `website` 内置网页资源爬虫，避免两个 Google 入口冲突。
- Google 搜索增加 0–300 秒“验证等待”，默认 60 秒。可见浏览器出现人机验证时允许手动完成，界面显示剩余时间；一旦检测到候选链接会提前继续。
- 下载类型增加“说明”按钮，并按平台解释是否生效；下载库按钮重新归位到“文件夹汇总”和“文件列表”各自标题栏。
- 通用目标预览增加标量抓取信息；Twitter HTML 资料卡继续显示主页、Skeb 和其他外链。
- 验证：`compileall` 通过，离线单元测试由 23 项增加到 25 项并全部通过；窗口在 `1080x680`、`1240x780`、`1500x845` 下初始化正常，Google 搜索与最终爬取按钮均可见。

## 2026-09-12 Google 辅助搜索到网页爬取

- 当前范围调整为 Google 相似图片辅助搜索链路，Twitter、Pixiv、JMComic 本轮不继续扩展。
- 桌面端新增“相似搜索”页签：选择单图、设置候选数、显示候选网站/URL、多选、全选、取消选择、在线预览和“爬取选中页面”。
- Google 只负责发现候选 URL。只有用户选择的页面才通过统一任务系统进入 `website` 网页资源爬虫，单个平台并发上限为 3。
- 新增软件内置网页资源爬虫：解析静态 HTML 的图片、视频、音频、`srcset`、Open Graph 媒体和直链；默认每页最多 50 个文件、单文件最多 200 MB。
- 安全边界：只接受 HTTP/HTTPS，拒绝 URL 内账号密码、本机地址和内网/保留 IP 字面量；HTML 最大 5 MB，下载使用临时文件后原子替换，并支持任务取消。
- 离线新增 4 项网页爬虫测试，覆盖相对 URL、去重、安全协议/本机地址拒绝，以及从选中页面下载嵌入媒体。
- 任务入库修正：内置爬虫已逐文件上报时，不再扫描整个输出根目录，避免并发候选任务把其他任务文件误记到自己名下；旧式未上报任务仍保留目录扫描回退。
- 完整回归：`python -m unittest discover -s tests -v` 共 23 项通过；`python -m compileall -q software_app tests run_software.py` 通过。
- 待真实网络验证：Google 人工验证流程、动态页面延迟加载、登录 Cookie、文件夹批量搜索和候选导出按钮。

## 2026-09-12 工作台界面重构验证

- 界面从白色骨架调整为高密度平面化工作台：固定深青侧栏、状态头部、统一表格/输入/按钮样式、橙色最终爬取动作。
- 相似搜索页增加查询图片真实预览、文件名、尺寸、大小、三步流程提示、候选/已选计数、斑马纹表格和明确的禁用状态。
- 使用 `D:\img_download\QQ图片20260909190459.png` 做只读预览验证，没有把图片复制到项目。
- 实际窗口检查通过：`1080x680` 下搜索按钮、44 像素高的紧凑图片预览、候选表格和爬取按钮均可见；`1240x780` 下图片预览为 141 像素高；宽屏请求 `1500x900` 时受测试屏幕限制为 `1500x845`，图片预览为 206 像素高。
- 三种尺寸下“开始任务、搜索相似页面、查询图片预览、候选表格、爬取选中页面”均位于窗口可见范围；长表格保留双向滚动。

## 2026-09-12 四平台内置爬虫第一轮

- Twitter 软件副本已同步到 `2026-09-08-media-routes-v12`，包含图片区/视频区分流、系统代理、退避重试、真实 PNG 转码和重复 JPG 清理。
- Twitter 主下载已由外部脚本子进程改为 `software_app/crawlers/twitter/service.py` 进程内调用；关注列表和主页采集仍是软件目录内的独立采集模块，后续继续服务化。
- 新增软件内置 Pixiv 爬虫、JMComic 爬虫和 Google Lens 页面线索搜索；三者标记为 alpha，旧项目目录不是运行时依赖。
- SQLite 文件索引新增 `source_id`、`author_id`、`author_name`、`chapter`、`published_at`、`tags_json`，并对旧数据库执行增量加列。
- 新增安全静态 HTML 快照渲染，动态 React/Vue 页面仍不在 TkinterWeb 中执行。
- 离线验证：`python -m compileall -q software_app tests run_software.py` 通过；新增 Twitter 非 200 重试回归后，以系统 Python 和项目虚拟环境分别执行 18 项测试，全部通过。
- 桌面冒烟验证：窗口可在 `1080x680`、`1240x780` 和宽屏尺寸初始化并自动关闭；测试机器屏幕将 `1500x900` 高度限制为 845 像素，布局未在初始化阶段报错。
- 真实网络待测：Twitter 需要有效 Cookie 和浏览器；Pixiv 登录限定内容需要 Cookie；JMComic 受域名、认证和限流影响；Google Lens 可能需要人工验证。未经真实网络验证的模块不得从 alpha 改为 ready。
- Pixiv 最小在线搜索探测：沙箱代理连接被拒绝；授权使用本机网络后直连 `www.pixiv.net:443` 在 10 秒连接阶段超时，未获得响应，因此没有执行真实响应解析或下载。JMComic 未重复进行相同网络条件下的探测。
- 项目虚拟环境已按 `requirements-app.txt` 补齐依赖并安装 `tkinterweb 4.25.4`，实际窗口确认 `html_enabled=True`，安全静态 HTML 快照可加载并正常关闭。
- 复核上游 Twitter v12 时发现并修复非 200 响应重试路径读取已置空对象的问题；修复仅在软件自有副本中，原目录未改动。

## 当前第一版状态

已完成：

- 本地桌面入口：`python run_desktop.py`
- 命令行入口：`python prototype.py`
- 通用核心层：`app_core/`
- 推特/X 适配器：`adapters/twitter_adapter/`
- SQLite 本地库：`data/content_downloader.db`
- 本地文件索引和基础预览：图片可内嵌预览，视频/音频/文本可打开文件

第一版采用 Tkinter，是因为当前环境已有 Tkinter 但没有 PySide6。后续正式桌面软件可以迁到 PySide6 或 Tauri。

## 每次继续开发前先跑的基础检查

在项目根目录运行：

```powershell
python -c "import compileall, py_compile; ok=True; ok=compileall.compile_dir('app_core', quiet=1) and ok; ok=compileall.compile_dir('adapters', quiet=1) and ok; ok=compileall.compile_dir('desktop_ui', quiet=1) and ok; py_compile.compile('prototype.py', doraise=True); py_compile.compile('run_desktop.py', doraise=True); print('syntax ok' if ok else 'compile failed')"
```

预期输出：

```text
syntax ok
```

再跑一次不联网的预览测试：

```powershell
python prototype.py preview '@example' -t 1,2
```

预期能看到：

```text
target: https://x.com/example/media
description: 下载类型: 图片, 视频
```

注意：PowerShell 里 `@用户名` 要加引号，否则可能被当成 PowerShell 特殊语法。

## 桌面软件冒烟测试

运行：

```powershell
python run_desktop.py
```

手动检查：

1. 窗口能正常打开。
2. 左侧输入 `@example`，点击“预览”，能显示规范化目标。
3. 右侧“本地内容”能列出已索引文件。
4. 选中图片文件时，能在预览区显示；如果格式不支持内嵌预览，会显示提示。
5. 选中视频/音频文件后点击“打开文件”，能调用系统默认程序打开。
6. 点击“刷新本地库”，状态栏显示本次索引数量。

## 真实爬取测试

真实爬取前确认：

- `推特爬虫/X_cookie.json` 是可用登录态。
- `推特爬虫/chromedriver.exe` 与本机 Chrome 版本匹配。
- 当前 Python 环境能导入 Selenium：

```powershell
python -c "import selenium; print(selenium.__version__)"
```

建议先用小目标测试，避免第一次扫太多内容：

```powershell
python prototype.py download '@目标用户名' -t 1 --output-dir '.\推特爬虫\test-output'
```

观察点：

- 控制台能输出 Selenium/爬虫日志。
- 下载目录生成图片或其他媒体文件。
- `data/content_downloader.db` 里能看到任务和文件记录。
- 再打开 `python run_desktop.py`，本地内容列表能看到新文件。

如果真实爬取失败，优先排查顺序：

1. Cookie 是否过期。
2. Chrome 和 chromedriver 是否匹配。
3. X/Twitter 页面结构是否变化。
4. 网络环境是否能访问目标。
5. 原脚本 `推特爬虫/twitter_Crawler_2.py` 单独运行是否正常。

## 已知限制

- 当前推特适配器是通过子进程调用旧脚本，进度粒度依赖旧脚本输出。
- 取消任务只是终止子进程，Selenium/Chrome 残留进程需要后续增强清理。
- Tkinter 内嵌图片预览对 jpg/webp 支持不完整，正式版应使用 Pillow 生成缩略图。
- 当前本地库扫描按文件扩展名识别内容，还没有作者、来源、标签等结构化信息。
- SQLite 已记录任务、日志和文件，但还没有做完整的任务详情页。

## 下一步开发顺序

建议按这个顺序推进：

1. 增加缩略图缓存。
   - 使用 Pillow 生成统一尺寸缩略图。
   - 列表改成缩略图网格或带缩略图的表格。

2. 把推特子进程封装改成内部 API 调用。
   - 直接调用 `download_one_target`。
   - 进度、取消、错误处理会更细。
   - 便于后续多任务队列。

3. 做任务详情页。
   - 显示当前任务目标、状态、输出目录、日志、已下载文件。
   - 支持失败任务重试。

4. 接入 JMComic。
   - 新增 `adapters/jmcomic_adapter/`。
   - 复用 `CrawlerAdapter`、`TaskManager`、`AppStorage`。
   - 下载完成后进入同一个本地库。

5. 正式桌面 UI 技术选型。
   - 如果优先稳定和本地原生：PySide6。
   - 如果优先以后复用网页技术：Tauri + 前端。
   - 当前 Tkinter 版本保留为原型和测试入口。

6. 打包 Windows exe。
   - 确认依赖、chromedriver、cookie、配置目录怎么随包分发。
   - 用 PyInstaller 或 Nuitka。

7. 再考虑手机应用。
   - 本地桌面先做成稳定核心服务或本地 API。
   - 手机端可以 Flutter/React Native，先作为远程控制和浏览器，不直接承担复杂爬虫。

8. 最后考虑网站和联网。
   - 做账户、远程任务、云端同步前，先把本地数据结构和插件接口稳定下来。

## 下次接着做时的推荐任务

优先做“缩略图缓存 + 图片预览增强”。这是最直接提升本地软件体验的一步，也不会大幅改动爬虫逻辑。

推荐目标：

- 新增 `app_core/thumbnails.py`
- 生成 `data/thumbnails/` 缩略图缓存
- 桌面列表显示缩略图
- jpg/webp/png/gif 都尽量能预览

完成后再推进推特适配器内部化。

## 油猴脚本 1.2 状态和后续记录

- 已核对 `油猴脚本/` 的独立远端仓库 `https://github.com/xiaohaoyiqu/tempermonkey-scripts`：本地 `main`、`origin/main` 与远端 HEAD 均为 `98ef75d Add complete 1.2.0 archive`。桌面端主仓库地址为 `https://github.com/xiaohaoyiqu/fast-get-it`。
- 脚本仓库沿用根目录放当前推荐 `.user.js`、`1.x版本/` 保存同版归档的规则；脚本文件使用小写 kebab-case，文档使用中文 `#`/`##` 分节、表格、清单和带语言标记的代码块，版本变化写入 `CHANGELOG.md`。
- 已查阅 `E:\aimodel`：`chrome-devtools-mcp/` 与 `agent-skills/skills/browser-testing-with-devtools/` 提供真实浏览器 DOM、控制台和截图验证思路；`browser-use/` 可作自动化参考；`zhihu-cli-skill/` 是知乎开放平台/本人数据 CLI，不是页面 DOM 脚本依赖。本轮没有安装、配置或调用其中任何服务，也没有读取账号凭据。
- Zhihu Reading Helper 准备升为 `1.2.0`：按钮轮新增“收父评 / 展父评”和“收子评 / 展子评”。基于提供的首页与问题页评论 DOM，父评论定义为 `.Comments-container` 内不嵌套在另一条评论 `div[data-id]` 下的评论，子评论为嵌套评论；每条须拥有自己的 `.CommentContent`。识别不依赖随机 `css-*` 类名。
- 两类评论均为手动切换：收父评会隐藏父评论及其子回复，收子评仅隐藏嵌套子评论；同一 SPA 路由中新加载的评论继承当前状态，切换路由会恢复默认显示。待在知乎首页、问题页和回答详情页实际验证嵌套回复加载与按钮恢复。
- 1.2.0 后续修正：轮盘一级入口纵向排列为“记录、历史、评论、回答、导航”。回答分类先选择“当前、全页”，再在被选按钮上方显示“收起、展开”；当前范围的展开只影响当前回答，全页范围才使用原有全页展开。评论分类只提供“父评、子评”，选择其中一种后在下方显示“收起、展开”。点击评论入口会直接检测并按需触发当前回答的知乎原生“X 条评论 / 收起评论”按钮；回答收起只识别原生 `data-zop-retract-question`/“收起”，显式排除“收起评论”，并兼容回答详情页的固定底栏。收起父评后会立刻禁用子评操作，父评展开后按实际子评收起状态恢复。二级操作均以用户点击的范围/类型按钮为定位基准，而不是主按钮。所有页面点击、脚本操作、DOM 增量更新和评论异步展开后均会防抖刷新全部可见轮盘按钮的可用态与文字。版本号仍为 `1.2.0`。

- `油猴脚本/` 已作为独立 Git 仓库推送到 `https://github.com/xiaohaoyiqu/tempermonkey-scripts`。根目录保留推荐安装版本，`1.0版本/`、`1.1版本/` 分别保存对应历史归档。
- Zhihu Reading Helper 已升到 `1.1.0`：历史面板支持固定问题、取消固定和清空未固定记录；“收起”拆分为“收当前”和“收全页”；轮盘按钮会根据当前页面和可操作内容显示禁用态与提示；“展开/阅读全文”只在 `/question/{id}` 和 `/question/{id}/answer/{id}` 问题详情页可用。
- Zhihu Reading Helper 仍优先调用知乎原生 `data-zop-retract-question`/“收起”和 `ContentItem-expandButton`/“阅读全文”；没有原生按钮时才使用脚本兜底收起，并插入脚本自己的“阅读全文”按钮。按钮轮继续避开右下角“看山”和回到顶部按钮。
- Instagram Media Downloader 标为 `1.1.0`：下载入口改到点赞/评论/分享操作栏，不再放在三点按钮左侧，避免遮挡关注按钮和触发 React hydration 报错；点击后先选择“立即下载”或“添加到队列”。队列最多 30 项，支持全选、取消全选、单选、删除、按类型下载、下载全部和清空；成功处理后自动移除队列项。
- Bluesky Media Downloader 标为 `1.1.0`：帖子按钮和页面级入口保留，点击时通过 Bluesky 公共 API 补全媒体。图片按 PNG，普通视频按 MP4，GIF 视频按 GIF，普通视频可抽 MP3。选择框默认不勾选资源，全选只负责选中全部，另有取消全选。
- Instagram 和 Bluesky 队列中的“按类型下载”按媒体类型处理：图片只下载 PNG，不参与视频转 GIF 或抽 MP3；普通视频可 MP4/MP3/GIF；GIF 图片保持 GIF，GIF 视频可转 GIF。
- 已执行离线语法检查：`node --check` 覆盖根目录三个脚本和 `1.1版本/` 三个脚本。Instagram 浏览器侧已确认可以正常加载；Instagram 控制台的 `Permissions-Policy`、`Document-Policy` 和 Self-XSS 提示属于站点日志，不作为脚本错误处理。
- 待浏览器继续实测：Bluesky 首次授权 `public.api.bsky.app`、`bsky.social`、`cdn.bsky.app`、`video.bsky.app`、`video.cdn.bsky.app` 后，在首页、个人主页、帖子详情页、搜索结果页分别验证图片、普通视频、GIF 转换和 MP3 抽取。当前脚本只直接封装常见 fMP4 HLS；如果 Bluesky 返回其他 HLS 分片形式，会提示并打开原链接。
- 已记录但未实现的 Zhihu 后续功能：问题页阅读进度，例如当前第几个回答；隐藏相关推荐、热榜或侧栏的开关。

## 最新需求后的开发顺序

以下顺序覆盖本文前面的旧版“下一步开发顺序”：

1. 固定新版 `software_app/` 为主入口，旧版只做回归参考。
2. 先完善统一任务、资源元数据、目录、历史记录和 SQLite 索引。
3. X/Twitter 关注列表导入/导出和用户搜索已完成；继续完善预选列表、推文 URL 和成功/失败汇总。
4. 把图片/视频/音频转换、缩略图、PDF、长图、压缩/解压缩和清理做成公共服务。
5. 接入 JMComic：搜索、预览、最多 15 项队列、最多 3 个并发下载。
6. 接入 Pixiv：收藏夹搜索、日期筛选、作品类型和 AI 过滤。
7. Google 相似图片候选选择、文件夹批量、导出与网页资源爬取链路已完成第一版；下一步补动态页面资源提取和跨页递归。
8. 单独设计 E-Hentai/ExHentai 的代理提示、收藏、压缩包和种子流程。
9. 最后再接入 Bluesky、Instagram、油猴脚本联动和手机端 API。

每个阶段都要同时补充：

- 正常任务、重复任务、取消、失败、重试和断点行为。
- 资源入库、目录命名、历史记录和清理行为。
- 平台限流、Cookie 缺失、代理失败和媒体格式异常的提示。

## 2026-09-13 Pixiv 限制内容、FANBOX 与 JMComic

- Pixiv 设置增加 R-18 二次提示和浏览设置页入口。软件只能发送 R-18 筛选，不能可靠读取或代改账号的年龄限制开关；缺少 PHPSESSID 时会在搜索前拒绝并说明原因。
- FANBOX 设置增加“检查续费状态”。无支持方案时明确显示无需续费；活动方案优先使用接口返回的付款/到期日期，接口不返回时显示本月月底和下月 1～5 日的规则窗口，并标记为规则推算。便利店预付等非自动续费方式仍以 FANBOX 支付页为准。
- FANBOX 候选和详情显示付费门槛；下载遇到 `isRestricted` 时跳过并提示支持或续费，全部受限时任务失败而不是空完成。
- JMComic 的分类/排行和收藏夹由规划入口改为实际入口。设置页可配置域名、模糊/精确名称匹配、分类、排序、时间、收藏夹用户名和目录 ID；关注/候选页可导入 Cookie JSON。搜索结果解析封面用于预览，收藏夹验证登录提示，HTTP 401/403/429 提供可操作的错误信息。
- 新增公共图片后处理模块，JM 章节可选择 PDF、长图或两者，按最多 30 张分组；默认不合成以保护低配置设备。JM 作品目录追加最多 5 个标签，检测到分割图时明确显示正在还原。
- JM 作品详情的 Open Graph 封面进入统一预览，默认随作品保存为统一图片格式；封面失败只发出警告，不会丢弃后续漫画章节。
- 新增离线回归覆盖 FANBOX 无方案、规则日期、精确接口日期、Pixiv R-18 缺 Cookie、JM 分类参数、封面、精确搜索、收藏夹 Cookie 和 30 张后处理分组。完整测试见本轮最终记录。

## Git 配置记录

项目根目录已初始化本地 Git 仓库，`origin` 指向 `https://github.com/xiaohaoyiqu/fast-get-it.git`。远端当前没有 HEAD，主仓库本地也还没有首个提交。

当前状态：

- 默认分支为 `main`。
- 尚未创建提交记录。
- 尚未执行 `git add`、`git commit` 或 `git push`。
- 已配置 `origin`，没有执行上传。
- 已添加根目录 `.gitignore`。
- 已将当前项目目录加入 Git 的 `safe.directory`，避免 Windows 目录所有权检查阻止本地命令。
- `油猴脚本/` 是独立 Git 仓库，`origin` 指向 `https://github.com/xiaohaoyiqu/tempermonkey-scripts.git`，主仓库会忽略该目录。

`.gitignore` 当前排除：

- Python 缓存、`.venv`、IDE 配置和构建产物。
- 整个 `data/software_app/` 运行数据目录，包括数据库、Cookie、浏览器资料和下载记录。
- X/Twitter Cookie、其他 Cookie JSON 和 ChromeDriver。
- 推特爬虫已经生成的作者媒体目录。
- 独立版本管理的 `油猴脚本/` 工作副本。

本地 Git 使用约束：

1. 默认只执行 `git status`、`git diff`、`git log` 等本地查看命令。
2. 只有明确要求时，才执行 `git add`、`git commit`。
3. 远程地址已经配置；只有明确要求上传时才执行 `git push`。
4. Cookie、数据库、下载记录和本地媒体不能提交到版本库。

### 2026-09-13 Pixiv 回归

- 从 `1.txt` 固化作者名称、`i.pximg.net/user-profile` 头像及 `/jump.php?url=` 外链的离线解析测试。
- 新增作者昵称搜索、小说收藏分页、Premium 浏览历史 HTML 去重、作者资料/头像缓存和账号历史清理测试。
- 新增 Google 候选只接受严格 `pixiv.net/artworks/{id}` 的显式 Pixiv 路由测试；原有通用网页路由保持不变。
- 聚焦测试：`tests.test_pixiv_extended` 18 项通过；`tests.test_native_crawlers` 的 Pixiv 作者预览回归已修复。完整测试结果见本轮交付记录。
- 真实作者页只读验证已尝试两次：沙箱内命中不可用的 `127.0.0.1:9` 代理，放开沙箱后直连 `www.pixiv.net:443` 仍连接超时。因此本轮不声明真实联网已通过；Cookie 值未输出，离线 `1.txt` 与模拟会话回归均通过。
- 最终执行 `python -m unittest discover -s tests -p "test_*.py"`：129 项全部通过。

### 2026-09-13 Pixiv 关注、实体解码与历史回归

- 新增回归：关注缓存可直接提供作者本地预览，不能隐式发起网络请求。
- 新增回归：Pixiv 每页关注结果在 UI 回调之前已经写入缓存；取消或后续请求失败不会丢失该页。
- 新增回归：作品标题、简介、作者名和标签会解码 HTML 实体，包含二次转义样例。
- 新增回归：在“标题 / 简介”范围输入具体 `/artworks/{id}` 时，任务选项和历史种类修正为“作品 ID”，并保留 `requested_content_scope` 用于确认提示。
- 执行 `python -m unittest discover -s tests -v`：132 项全部通过。
- Windows Tk 烟雾测试：窗口实际尺寸 `1080x680` 时主 Notebook 可用宽度为 718；Pixiv“关注画师”会禁用顶部直接开始按钮并显示“从列表下载”，历史列宽和对齐可正常建立。`1240x780` 和宽屏尺寸也完成创建/销毁检查。
- 本轮未重复声明 Pixiv 真实联网通过；网络能力仍需在 Pixiv 可连接、Cookie 有效的环境验证分页总数、私密关注和 Premium 历史。

### 2026-09-18 黑名单公开样例与相似图片候选回归

- 以 X 的 `@pixiv` 官方账号帖子 `2023978485316768106`、`1915708135853236282` 和 Bluesky 官方账号帖子 `3mu3jzayuys2k` 测试作者规则、单帖规则、Google 候选状态及解除屏蔽。公开页面经网页检索核对；测试只读取公开地址，不向平台账号写入屏蔽操作。
- 修复相似图片候选链接检查后跳转地址未重新匹配黑名单的问题。Google 候选表、平台候选导出和“解除选中作品屏蔽”同时考虑原地址与最终地址；已检查到跳转至 Pixiv 作品页时，“交给 Pixiv 下载”使用最终作品地址。网页下载器在浏览器或 HTTP 跳转后再检查最终页面，命中时不写入媒体文件。
- 所有规则写入临时黑名单并在测试中逐条解除；测试结束时账号和作品规则均为空。工作台原本不存在 `data/software_app/blocklist.json`，测试结束后仍不存在。
- Windows 图片转换过程中遇到瞬时文件共享冲突时，重试临时文件重命名；增加模拟锁占用的回归测试。
- 执行 `python -m unittest discover -s tests -q`：177 项通过。以上是公开 URL 和模拟浏览器响应的集成回归；未使用个人账号、Pixiv Cookie 或真实 Google Lens 上传图片。

### 2026-09-19 X 单帖与 Google Lens 真实联调

- 使用公开的 [NASA 图片帖子](https://x.com/NASA/status/2040059770237849635)测试 X 原生单帖下载。现有登录 Cookie 只读注入临时 Chrome 配置，输出、下载记录和作者资料均保存在自动清理的临时目录；成功下载 1 张 PNG，文件名包含目标帖子 ID `2040059770237849635`。首次运行发现同一图片从页面和接口重复入队，修复后复测为入队 1、成功 1、重复候选 1，未下载推荐/回复帖媒体。
- 使用 Google 公共标志图片经当前代理实际上传到 Google Lens。当前网络返回 CAPTCHA 人机验证页，未得到可用候选；此前流程返回 0 个候选，现已改为明确的验证提示。真实候选提取、批量搜索与相似结果质量仍需在人工完成验证后复测。
- 用公开的 [Pixiv 官方 X 帖子](https://x.com/pixiv/status/2023978485316768106)检查 HTTP 可访问性和页面中的帖子 ID；没有改动 X、Pixiv 或 Google 的账号状态。新增 `scripts/live_twitter_post_smoke.py` 与 `scripts/live_similarity_smoke.py` 供重复执行只读临时目录测试。
- 公开 NASA 图片的 Google Lens 可见浏览器复测曾进入“全部”页并显示来源卡片；用户保存的 `图片/1.png` 也显示 Wikimedia Commons、Threads 和 Guardian 结果。`图片/2.png` 则显示切换到“外观匹配”后“视觉搜索内容已过期／图片未找到”，本机也重现了标签跳转后丢失匿名上传图片。现保持当前有效结果页，从标题或图片卡片提取来源，遇到过期页明确提示重新上传；不把 AI 概览当成已验证的相似图片结果。
- Google 代理出口多次触发 `/sorry/index` 人机验证，最后一次真实复测因验证页未取得候选，故无法宣称匹配结果质量已经稳定通过。离线回归 `python -m unittest discover -s tests -q` 为 185 项通过。截图和页面 HTML 诊断文件只用于本机查看，不保存账号状态。

## 2026-09-19 Pixiv 状态与 JMComic 补全

- Pixiv 平台插件状态改为 `ready`。JMComic 仍保持 `alpha`，因为当前默认站点经软件已保存代理返回 HTTP 403，无法完成真实搜索、登录收藏夹和下载的端到端验收；用户当前也无法提供浏览器中可访问的站点网址。
- 对照旧版 JMComic 解析与上游项目用法，修复带域名 URL 的 ID 提取、搜索精确匹配范围、重复页停止条件、缺失图片时的假成功、HTTP 200 拦截页提示和分割图默认阈值。图片先写 `.part` 再替换正式文件。补充旧式 `data-album` 章节标记解析、第一页在线预览入口与封面入口；首图预览也会按章节参数还原分割图。
- JMComic 设置页可对当前域名执行只读连接检查。候选页可编辑最多 5 个标签、移除条目，并通过现有 CSV/JSON 功能保存；候选标签会传入下载任务并用于漫画目录名称。JMComic 插件继续限制最多 15 项搜索候选及 3 路并发；站点不可达时保留明确错误。
- 完整离线回归 `python -m unittest discover -s tests -q` 共 196 项通过；`python .\run_software.py modules` 显示 Pixiv `ready`、JMComic `alpha`。JMComic 候选页在 1080×680、1240×780、1500×860 窗口下显示了标签、移除按钮及候选列表。

## 2026-09-19 JMComic 示例、登录边界、黑名单与窄窗口

- 新增 `平台/JMComic使用与登录.md` 和 `scripts/jmcomic_feature_smoke.py`。本地预览明确只识别目标及浏览器去向；在线漫画预览读取标题、作者文字、标签、章节、封面和第一页，章节预览读取图片列表。JMComic 当前不提供软件可验证的稳定作者用户 ID，因此不声称能取得作者账号资料、简介、关注或外链。
- 收藏夹明确要求 Cookie JSON、用户名和目录 ID；软件不保存密码。设置页记录 Cookie 的本地边界。“打开网页”对漫画进入详情/章节目录，对章节进入正文阅读页，对关键词进入搜索页；站点仍可按浏览器会话跳转登录、地区限制或验证页。
- JMComic 黑名单新增漫画 ID、章节 ID和规范化作者名三类规则。漫画和章节在任务创建前拦截；作者名在取得漫画详情后再次拦截。镜像域名变化不影响 ID 规则，作者同名或改名是名称规则的已知限制。
- 修复 1080×680 下左侧目标列表被资料卡挤没、相似搜索候选表被操作按钮挤没，以及外链下方按钮超宽的问题。资料卡改为独立滚动区；外链和黑名单按钮使用自适应两列；相似搜索把次要操作收进“更多操作”并采用紧凑按钮；任务详情按钮缩小，为日志保留高度。
- 本地功能示例通过，完整离线回归共 198 项通过。三种窗口实测：1080×680 下左侧目标列表 80 px、左侧日志 79 px、相似候选表 63 px、任务日志 114 px；1240×780 和 1500×860 下对应区域继续扩展。

## 2026-09-19 JMComic 浏览器会话与真实验收完成

- `1.txt` 的完整开发者工具请求/响应标头可直接导入。导入器从整份文本提取 Cookie，只保留允许列表内的浏览器请求头，并以首次出现值为准，避免后面的响应 `Cache-Control` 覆盖请求值；Cookie 标头不会重复写入浏览器头配置。
- JMComic 设置新增单行 User-Agent；当前请求通过匹配的 User-Agent、请求头、Cookie、软件代理及 `curl_cffi` Chrome 指纹建立会话。配置文件原子写入，重新导入不含请求头的 Cookie 文件时会清除旧浏览器头，避免错误复用旧验证环境。
- Cloudflare 偶发 HTTP 403 会等待 0.6 秒重试一次；持续 403 仍明确要求更新同一域名、代理出口和浏览器环境下取得的验证信息。
- 真实只读验证完成：首页连接正常；漫画收藏、追更、漫画观看记录均取得候选；小说收藏与小说观看记录页面有效且当时为空；站内屏蔽页解析 28 条标签规则。没有输出 Cookie、作品标题、作品 ID或账号浏览内容。
- 从真实漫画收藏候选完成详情、章节、首图和分割图解码；临时章节测试连续完成 248 页请求、还原和原子落盘后因样本过大主动停止，随后清理全部临时文件。该测试不写正式下载库，也不修改网站账号。
- JMComic 模块由 `alpha` 调整为 `ready`。`python .\run_software.py modules` 显示 Twitter、JMComic、Pixiv 均为 `ready`；本地功能脚本通过，完整回归为 208 项通过。

## 2026-09-19 JMComic 界面整理

- 设置页将 JMComic 标记为“可用”，并拆分为“站点与登录资料”和“搜索与下载”。登录区可直接导入 Cookie 或完整请求标头，分别显示 AVS 候选状态、浏览器请求头数量、账号用户名和站点连接结果，并提供本地使用说明入口。
- JMComic 候选页会按漫画收藏、小说收藏、追更、漫画记录和小说记录显示对应页签与读取按钮；每种搜索在界面中显示自己的用途说明。候选区同时展示本机登录资料状态，避免把 `cf_clearance` 误认为账号登录。
- 1080×680 下 JMComic 候选表保留 135 px 可见高度；设置区所有子控件均收在 632 px 内容宽度内并通过设置页纵向滚动访问。1240×780 下候选表为 235 px，1500×860 下为 300 px。
- `python -m unittest discover -s tests -q` 完整离线回归仍为 208 项通过。

## 2026-09-19 通用浏览器登录与 Cookie

- 设置页新增统一的可见浏览器登录入口，覆盖 Twitter/X、Pixiv、FANBOX、Instagram 和 JMComic。登录、验证码、双重验证与站点挑战都在官网页面中完成，软件不接收或保存账号密码；可随时停止，未取得必需 Cookie 时不会覆盖旧文件。
- 保存层按现有适配器格式转换：X 保留 Selenium Cookie 列表；Pixiv 与 FANBOX 合并名称和值，避免两次获取互相覆盖；Instagram 检测 `sessionid`；JMComic 检测 `AVS` 并同步当前浏览器 User-Agent。Bluesky 公共读取不需要 Cookie，账号屏蔽名单继续使用一次性应用密码。
- 新增用户操作文档 `平台/浏览器登录与Cookie.md`，并从文档首页、项目 README、用户手册及 Bluesky/Instagram 说明互相链接。文档记录了代理、各平台完成标志、文件导入备用方式和 `instagram-cookie-generator` 的适用边界。
- 新增 5 项保存层回归，覆盖平台必需 Cookie、Pixiv/FANBOX 合并、X 列表格式、域名相似攻击和不完整登录不落盘。完整测试 `python -m unittest discover -s tests -v` 为 219 项通过。
- Windows Tk 在 `1080x680` 下完成设置页视觉检查：设置画布宽 680 px，登录卡片宽 656 px；首轮发现说明按钮被挤出，改为独立按钮行后“打开登录浏览器并获取”“使用说明”和状态文字均完整可见。ChromeDriver 检测通过；真实账号登录仍由用户在官网窗口中完成，本轮没有读取或输出个人 Cookie。
- 后续真实获取检查发现 Pixiv、FANBOX 和 JMComic 在登录前也可能发放 `PHPSESSID`、`FANBOXSESSID` 或 `AVS` 候选。捕获条件改为 Cookie 与平台验证同时满足：Pixiv 账号接口、FANBOX 支持方案接口、JMComic 当前用户名收藏页；访客会话不再提前关闭浏览器或覆盖旧文件。统一登录卡片新增文件导入，Twitter/X 可从普通 Chrome/Edge 的 JSON、Netscape 文件或 Cookie 请求头生成 Selenium Cookie 列表，规避 X 拒绝 WebDriver 登录。
- 当前旧 X Cookie 含 14 项并成功注入临时浏览器，能够只读识别已登录会话；没有输出账号名或 Cookie 值。现有新捕获文件的只读检查为 Pixiv/FANBOX HTTP 401、JMComic 账号页跳回登录，说明它们是此前错误完成条件保存的访客会话，需要在修复后重新获取；Instagram 含 `sessionid`，但本轮代理连接 Instagram 失败，尚未完成在线有效性确认。修复后完整回归为 221 项通过，1080×680 下新增三个操作按钮完整显示。

## 2026-09-19 JMComic AVS 直接获取修复

- 修复“登录完成后获取”另开可见空白页的问题。Windows 获取阶段不再启动 WebDriver，而是直接只读专用 Chrome 资料库，并用当前 Windows 账号解密 Cookie；专用资料启用会话恢复，登录完成后关闭全部专用窗口再读取，避免活动 Chrome 独占 Cookie 数据库。
- 只接受当前配置域名下的非空 `AVS`。确认成功后才原子更新 `cookies.json` 和匹配的 Chrome User-Agent；没有 `AVS`、资料库暂时锁定或解密失败时保留原文件并显示具体提示。
- 增加 Chrome v10/v11 AES-GCM Cookie、数据库第 24 版域名摘要、真实资料库只读解密和“有 AVS 才更新”的回归检查。Cookie 值不会写入日志或测试输出。
- `python -m unittest discover -s tests -q` 完整离线回归为 227 项通过；随后新专用资料成功取得非空 `AVS`，并完成账号漫画收藏页的人工在线只读验收。
- 用户提供的 `1.txt` 实际是失败日志而不是 Cookie 文件。结合进程和文件访问复现确认，失败时专用 Chrome 仍在运行并独占 Cookie 数据库；读取流程现明确要求关闭全部专用窗口，并将中文父目录继续保留在回归样例中。
- 新 Cookie 已包含非空 `AVS` 与 `cf_clearance`，但初次在线账号页验证返回 HTTP 403。对照捕获环境发现自动生成的 User-Agent 未采用 Chrome 的版本缩减形式，且只保存一个头；改为 `主版本.0.0.0` 并补齐 8 个安全导航请求头后，同一 Cookie、域名和代理成功访问首页及账号漫画收藏页，返回 1 个只读候选。

## 2026-09-19 全平台与相似搜索验收

- 新增 `scripts/ui_platform_visual_smoke.py`，使用真实 `SoftwareDesktop` 依次切换 Twitter、JMComic、Pixiv、Google 相似图片、网页资源、Bluesky 和 Instagram，并在 `1080x680`、`1240x780`、`1500x860` 三档保存相似搜索截图与控件边界指标。测试候选为本地虚拟数据，不触发下载。
- 相似搜索首屏顺序调整为“选图 → 图片预览 → 提示/搜索选项”；候选操作行移到结果表上方。三档窗口内图片预览、开始搜索、爬取选中页面、更多操作、目标输入、输出目录和类型输入均在窗口内。候选表可见高度分别为 63、180、245 px，最小窗口继续通过滚动访问候选。
- 真实只读联网验证：X 公开页面 HTTP 可达；JMComic 当前 Cookie 同时含非空 AVS 与 `cf_clearance`，首页可达且账号漫画收藏取得 3 个受限样本候选；Pixiv 公开用户资料、Bluesky 官方公开帖子和普通网页预览成功。验证不输出 Cookie、代理地址或个人收藏内容。
- Instagram 当前 11 项 Cookie 中存在 `sessionid`，主页请求返回 HTTP 200 和完整动态壳页，但不再提供现有静态解析器依赖的 Open Graph/资料 JSON，因此在线资料解析仍失败；模块继续保持 `alpha`，不能把“网络可达”写成“资料解析通过”。
- Google Lens 使用公共 NASA 图片完成真实上传，结果页被当前出口的 `/sorry/index` reCAPTCHA 拦截。软件正确识别并提示人工验证；验收后已按要求删除诊断截图和视觉测试输出，可由烟雾脚本重新生成。本轮不宣称真实候选数量或匹配质量通过。
- 命令行 `preview --live` 过去漏传 SQLite 中的软件代理，并会继承启动器环境代理。现与桌面端一致传入软件代理、重试和网页超时；通用 `requests.Session` 禁止隐式继承 `HTTP(S)_PROXY`，代理留空明确表示直连。
- 最终 `python -m unittest discover -s tests -q` 为 228 项通过，`compileall` 通过；模块状态为 Twitter/JMComic/Pixiv/Bluesky `ready`，Google 相似图片/网页资源/Instagram `alpha`。

## 2026-09-19 X 单帖与 Instagram 动态页面补全

- X 精确单帖在浏览器页面只返回动态壳或加载超时时，先读取公开的单帖媒体数据，再复用原下载、格式转换和命名流程。公开 NASA 图片单帖实测入队 1 张并成功下载 1 张，没有下载推荐内容。
- Instagram 静态 HTML 不含资料数据时，在线预览和下载会自动切换到已导入 Cookie 的浏览器渲染；只接受 Instagram CDN 媒体，并继续写入 Instagram 下载目录和文件索引。浏览器 Cookie 加载同时兼容当前保存的名称和值字典格式。
- 真实访问 Instagram 官方公开账号完成 1 张有效图片下载，Pillow 解码检查通过。随后通过正式 `TaskManager` 在临时数据库和临时输出目录完成任务：状态与回调均为 `completed`，生成 1 条图片记录、1 条 ZIP 记录及 1 条历史记录；测试结束后输出目录自动删除，未修改正式下载库。
- Instagram 模块状态由 `alpha` 调整为 `ready`。完整离线回归为 231 项通过，`compileall` 通过；临时测试脚本、输出目录和 ChromeDriver 进程均已清理。

## 2026-09-19 E-Hentai / ExHentai 首轮接入

- 对照旧需求和 E-Hentai 官方 Wiki，新增独立 `ehentai` Adapter/Crawler。支持表站/里站画廊 URL、官方元数据 API、表站/里站关键词候选、登录收藏夹候选、在线预览、AI 标签过滤、缩略图、独立 Cookie 和精确画廊黑名单。搜索串行且至少间隔约 3 秒，任务并发上限为 1。
- 下载任务保存 UTF-8 JSON 元数据，并在可用时保存最新一个 `.torrent` 原始文件；种子先检查大小和 bencode 字典头。直接种子 URL 不写元数据或日志。软件不启动 P2P，也不自动申请可能消耗 GP/Credits 的官方 ZIP。旧需求中的通用 `.eh` 格式未在官方资料中确认，因此没有自行编造。
- 当前软件代理实测表站首页 HTTP 200 且返回完整 HTML；没有登录 Cookie 时里站 HTTP 200 但正文为空，客户端明确判为权限/会话失败。真实表站关键词搜索返回 3 个规范候选；官方 API 示例画廊通过正式 `TaskManager` 保存 1 份元数据和 1 个有效种子描述文件，任务、完成回调、文件索引和历史记录均正常。
- `1240×780` 实际截图检查新增平台工作台；缩短说明后候选页平台选择、目标输入、搜索种类、操作按钮和候选表均正常显示。相似搜索原有三档响应式检查继续通过。
- 完整离线回归为 238 项通过，`compileall` 通过。真实联调目录、种子、元数据、临时数据库、探测脚本和 UI 截图均已删除；没有写入正式下载库，也没有输出 Cookie、代理地址或个性化种子 URL。

## 2026-09-19 E-Hentai 表站/里站账号隔离补强

- 浏览器登录入口拆为 **E-Hentai 表站** 与 **ExHentai 里站**，分别保存 `e-hentai-cookies.json` 和 `exhentai-cookies.json`；两个账号可不同，不合并、不回退、不复用。旧版未区分站点的 `cookies.json` 只提示，不自动读取或迁移。
- 里站入口在打开浏览器前明确提示尽量使用非亚洲代理/VPN并保持同一出口。论坛登录后自动导航到 ExHentai；只有 `ipb_member_id`、`ipb_pass_hash`、`igneous` 齐全且里站正文非空才保存。
- 请求层把表站 Cookie 限定为 `*.e-hentai.org`、里站 Cookie 限定为 `*.exhentai.org`；缩略图 CDN 与种子站不携带任一账号 Cookie。增加双账号不同值、跨域不发送、里站缺少 `igneous` 拒绝保存和空白页拒绝通过的回归测试。
- EH 专项回归 23 项、全项目离线回归 241 项和 `compileall` 均通过。未创建正式 EH 下载、种子、元数据或 Cookie；测试临时目录均由用例退出时清理。

## 2026-09-19 E-Hentai 普通 Chrome 登录兜底

- EH 登录从设置页通用 WebDriver 按钮切换为和 JMComic 一致的两阶段普通 Chrome 流程。登录阶段不带 WebDriver/远程调试参数；表站和里站在统一 `manual_login_profiles` 根目录下使用 `ehentai`、`exhentai` 两份持久资料。
- 表站普通 Chrome直接准备 `uconfig.php`；里站同时准备论坛登录、ExHentai 首页和设置页。关闭全部专用窗口后，Windows 直接读取 Chrome Cookie 数据库，检查必需字段，并以同一代理和浏览器 User-Agent 实际验证表站设置页或非空里站页。
- Cookie 只有在在线验证成功后才原子更新；缺少 `igneous`、仍返回登录页、里站为空、浏览器数据库锁定或网络验证失败均保留旧文件。验证成功后还会按账号保存并复用普通 Chrome User-Agent。专项回归增加独立资料目录、无自动化启动参数、里站域优先、浏览器身份分离和验证失败不覆盖测试。
- `uconfig.php` 自动写入暂缓：先通过真实登录页面确认字段、防伪参数和权限，再只对白名单内的显示/搜索/收藏分类选项提供差异确认。额度重置、Archive、Hath/GP/Credits 和捐赠不进入软件写操作。
- EH/登录专项回归 25 项、全项目离线回归 243 项和 `compileall` 均通过。`1240×780` 实际截图确认表站/里站只显示普通 Chrome 两阶段按钮，四个操作按钮均在窗口内；截图随后全部删除。截图脚本同时修复了 Windows 前台窗口遮挡时抓到其他窗口的问题，每次捕获前会重新置顶本应用。

## 2026-09-19 普通 Chrome 复用与 Bluesky 关注管理

- 增加统一 `manual_login_profiles` 根目录。JMComic、EH 表站、EH 里站及以后需要普通浏览器登录的平台共用同一 Chrome 程序和底层读取/解密代码，但按平台/账号隔离用户资料，避免 EH 不同账号或不同站点 Cookie 覆盖。检测到既有 `jmcomic_manual_profile` 时原地兼容，不复制、不删除登录资料。
- Bluesky 用户候选增加单项关注与取消关注。写入使用官方 `app.bsky.graph.follow` 仓库记录和当前账号 PDS；执行前明确确认，应用密码与访问令牌只驻留工作线程内存，设置只保存非敏感 handle。已有状态会识别并避免重复写入。
- 新增关注创建、精确删除已有记录和重复操作不写入测试。全项目离线回归更新为 246 项通过，`compileall` 通过。

## 2026-09-19 E-Hentai 单项收藏管理

- EH 画廊候选增加 **加入 EH 收藏**、**移出 EH 收藏**。加入支持分类 `0`–`9` 和 UTF-8 最多 200 字节注释；写入前明确确认，提交后重新读取分类、注释或移除状态，HTTP 200 但状态未变化不会误报成功。
- 表站与里站仍由画廊 URL 严格路由到各自 Cookie/User-Agent 会话，禁止跨站收藏写入。重复加入相同分类/注释、重复移除均不产生写请求。
- 新增收藏新增/更新/移除、写后核验失败、注释长度和跨站会话测试。`1240×780` 实际截图确认两个收藏按钮在首屏且单选画廊后可用；测试截图在验收后删除。
- 完整离线回归更新为 249 项通过，`compileall` 通过。

## 2026-09-19 ExHentai 顺序登录与表站账号实测

- 修正里站首次登录流程：不再在登录前并行打开论坛、表站设置和空白里站。现在先用里站独立资料打开普通登录页，完成表站/论坛登录后由用户点击 **登录后进入里站**，同一 Chrome 资料再访问 ExHentai 以取得 `igneous`；确认正常后关闭窗口并获取。缺少 `igneous` 时错误提示会直接说明这三个步骤。
- 使用用户刚获取的正式表站 Cookie 做隐私安全的只读在线检查：必需字段、`uconfig.php` 精确落点、收藏夹候选、单项收藏状态均通过。检查未输出 Cookie、代理、标题、画廊 ID/令牌、注释或分类内容，也未修改线上收藏。
- 收藏弹窗解析增加账号当前的 10 个自定义分类名称。点击加入收藏时会先异步读取分类名称及现有分类/注释，再显示选择和最终确认；真实表站页面已确认十个分类名称均能解析。
- 增加 EH DevTools 请求标头的验证导入：兼容 `Cookie:` 与值分行、请求头后附响应头的文本；只读取 Cookie 和可用浏览器 UA，使用软件当前代理验证目标站点后才原子保存。用户提供的里站抓包包含四个 Cookie 且三项必需字段齐全，真实验证、保存、正式里站首页、空收藏夹、普通搜索、官方元数据画廊预览、单项收藏状态及十个分类名称读取均通过，未执行收藏写入。
- 请求头转换扩展到 Twitter/X、Pixiv、FANBOX、Instagram、E-Hentai 表站、ExHentai 里站和 JMComic。转换前按平台检查必要 Cookie，输出文件不会覆盖旧 JSON；EH 从原 TXT 保留可用 User-Agent 并在线验证，JM 从原 TXT 保留允许列表内的安全请求头，避免只导入 Cookie 后丢失 Cloudflare 会话环境。
- EH 功能随后完成整套复测：表站 `uconfig.php`、表/里站收藏夹、普通搜索、官方元数据画廊预览、真实缩略图解码、单项收藏状态及 10 个自定义分类名称均通过；里站收藏夹当时为空，按设计回退到普通搜索样本验证。全程只读，没有加入、修改或移除线上收藏。
- 新增隐私安全的 `scripts/eh_live_download_smoke.py`。表站实际保存元数据与 1 个有效 bencode 种子，里站实际保存元数据；两者随后均通过公共 ZIP 压缩、成员数量与完整性校验，临时下载和压缩文件自动删除。官方付费 ZIP 路径另增回归，确认在网络请求和本地落盘前拒绝自动购买。
- 用户补充的真实画廊/阅读页 HTML 片段确认：画廊缩略图区用 CSS 背景图并链接到 `/s/...`，正文展示图位于阅读页 `<img id="img">`，下一页由 `a#next` 指向。下载器现逐页保存正常展示图，只允许官方 `hath.network`/`ehgt.org` HTTPS 节点，验证图片后入库；`fullimg.php` 原图和可能消耗 GP/Credits 的 Archive ZIP 均不请求。
- EH 种子改为独立持久化开关，默认关闭；新任务及重试任务只有在用户手动开启后才保存最多 1 个 `.torrent`，仍不启动 BT/P2P。使用用户片段所属画廊实际下载 1 张展示图和元数据，完成 ZIP 成员/完整性校验后删除全部临时内容。
- 完整离线回归更新为 259 项通过，`compileall` 通过；1240×780 实际截图确认 EH 种子开关完整显示并默认关闭，EH 表/里站和 JM 转换按钮完整显示，相似图片页三档尺寸指标保持正常。含明文 Cookie 的原始抓包及本轮视觉测试截图均在验证后删除。

## 2026-09-19 E-Hentai 种子解析与 aria2 接入

- `.torrent` 不再只检查字典头：新增有界 bencode 解码，按原始 `info` 字节计算 info-hash，核对单/多文件总大小、分片大小与分片数量，并拒绝重复字段、非规范整数、尾随数据、危险根名称和路径穿越。无效响应会立即删除，不进入下载库。
- 选择 aria2 作为可选开源 BT 引擎：保存种子和下载种子内容是两个独立、默认关闭的设置。64 位 Windows 可从官方 Release 安装固定版本，安装前验证固定 SHA-256，只提取唯一的 `aria2c.exe`；也支持系统 PATH。执行使用参数数组且不经过 shell，控制台输出丢弃，任务取消会终止进程，完成后停止做种。
- 设置页明确提示 P2P 可能暴露公网 IP 且下载期间可能上传。自动化测试只使用伪进程与伪内容验证安装、命令、取消和 EH 文件登记，不会擅自加入真实 P2P 网络。
- 完整离线回归为 268 项通过，`py_compile` 通过。1240×780 脱敏截图确认种子保存、aria2 下载两个开关均默认关闭，检测/安装按钮和风险说明完整显示；截图在人工查看后删除。当前执行环境无法连接 GitHub 443，因此未把官方二进制留在项目中，不能把真实安装写成成功。

## 2026-09-19 Windows 发行、aria2 与浏览器驱动验收

- 用户提供的 `aria2-1.37.0-win-64bit-build1.zip` SHA-256 为 `67D015301EEF0B612191212D564C5BB0A14B5B9C4796B76454276A4D28D9B288`，与代码固定值一致。已从本地官方包实际安装 `aria2c.exe` 和允许的许可/README 文件，版本为 1.37.0；本机 localhost WebSeed 合法合成种子下载完成，文件哈希一致，测试临时数据退出时自动清除。BT 命令禁用 DHT、IPv6 DHT、PEX 和 LPD，完成后停止做种。
- ChromeDriver 检测现会实际运行驱动并比较 Chrome/Driver 主版本；无效或不匹配时进入自动更新。在线验收从伪造的无效驱动开始，自动查询 Chrome for Testing，选择并安装与本机 Chrome `122.0.6182.0` 精确一致的 ChromeDriver `122.0.6182.0`，再次检测通过。网络读取增加一次瞬时失败重试，下载后增加三次执行校验；失败不会保留无效文件。构建机 PATH 中已有的匹配驱动会迁入运行数据目录，但发行包明确排除 ChromeDriver。
- 新增 PyInstaller `onedir` 发行配置、兼容 Windows PowerShell 5 的 ASCII 构建脚本、内置 FFmpeg、aria2 官方 ZIP、本地用户数据目录和 Inno Setup 安装脚本。正式 `dist\欲求达` 构建约 199.8 MiB，包含桌面版与 CLI；递归检查 ChromeDriver 数量为 0，aria2 ZIP 哈希保持一致。打包版 8 个平台模块装载、Driver 版本检测、aria2 本地包安装、FFmpeg 7.1 启动和隐藏 GUI 启动/退出烟测均通过。本机未安装 Inno Setup，因此本轮只验证了安装脚本内容，尚未生成 `Setup.exe`。
- 完整离线回归更新为 274 项通过，`compileall` 通过。所有驱动下载、直接探测、旧临时发行目录、GUI 与 aria2 烟测目录以及 PyInstaller 中间 `build` 目录在验证后删除；正式 `dist\欲求达`、项目根目录官方 aria2 ZIP 和正式用户数据保留。
- Windows 图标统一改为根目录 `蔑视.png`：原图通过 PNG 完整性校验（2000×2000 RGBA），构建前确定性生成含 16/24/32/48/64/128/256 七档尺寸的 `packaging\欲求达.ico`。桌面 EXE、CLI EXE 和 Inno Setup 均引用该图标；从两个最终 EXE 反向提取的 32×32 Windows 图标内容一致并完成视觉检查，验证截图随后删除。

## 2026-09-19 仓库归属与文档整理

- 主仓库本地 `origin` 已指向 `https://github.com/xiaohaoyiqu/fast-get-it.git`。该远端地址可访问但尚无 HEAD；本地 `main` 也没有提交，本轮没有执行 `git add`、`git commit` 或 `git push`。
- `油猴脚本/` 保持独立仓库，`origin` 为 `https://github.com/xiaohaoyiqu/tempermonkey-scripts.git`，本地 `main` 与远端 `main` 均停在 `98ef75d`。主仓库通过 `.gitignore` 排除这份工作副本，没有改成 submodule。
- 根 README 重新写成安装与使用入口，增加平台状态、诊断命令、数据边界、BT 风险、Windows 打包和目录说明。新增 `仓库与目录说明.md`，记录两个仓库的分工、旧源码边界和发布前检查。写作参考了 `E:\aimodel\Humanizer-zh`、`human-writing`、`no-ai-slop` 与 `agent-skills/skills/documentation-and-adrs`，只保留可核验的命令、版本和限制。
- `.gitignore` 现在整体排除 `data/software_app/`，避免 Cookie、请求头、SQLite 和浏览器资料因文件名变化漏入版本库。旧爬虫和旧界面骨架也从主仓库待提交范围排除，但本轮没有移动或删除这些本机文件。
- 文档复核覆盖根 README 和 `文档/` 下 16 份 Markdown：本地链接缺失数为 0，代码围栏均成对。`E:\aimodel\human-writing` 的 `check_prose.py` 检查根 README 与仓库说明均通过；主仓库待提交范围收敛到当前实现相关的 145 个文件。油猴脚本工作区保持干净，两个仓库的 `origin` 均与指定 GitHub 地址一致。
